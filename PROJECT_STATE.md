# سجل حالة مشروع تطبيق الدراجات الهوائية (iraq_cycling)

## نظرة عامة على المشروع
تطبيق تتبع دراجات هوائية مختص بالعراق، Offline-First بالكامل،
يستهدف دراج شبه محترف يحتاج تحليلات أداء دقيقة. مبني بمطور واحد.

## المعمارية المتفق عليها
- Offline-First: كل الميزات الأساسية (GPS، BLE، السعرات، التحذيرات
  الصوتية) تعمل بدون إنترنت إطلاقاً.
- خرائط: flutter_map + بلاطات OpenStreetMap محلية لاحقاً
- BLE: flutter_reactive_ble لاحقاً لحزام نبض + عداد قدرة
- قاعدة بيانات: sqflite محلي
- إدارة حالة: provider

## خطة المراحل الكاملة
- Phase 1: نواة GPS Tracking - مكتملة (انظر السجل أدناه)
- Phase 1.5: عرض المسار على خريطة offline
- Phase 2: تكامل BLE + حساب سعرات حرارية
- Phase 2.5: الملف الشخصي للمستخدم (وزن/عمر حقيقيين للسعرات + إحصائيات عامة)
- Phase 3: تحذيرات مخاطر مرورية صوتية
- Phase 4: اشتراك مدفوع

## قواعد العمل الثابتة
- لا git commit أو push تلقائي أبداً
- لا تجاوز نطاق المرحلة الحالية
- عند أي غموض تقني، توقف واطرح السؤال

---

## سجل الجلسات

### جلسة 2026-08-14 (الثالثة) - شحن قاعدة بيانات التوجيه كأصل Flutter (RoutingGraphSeeder)

**نطاق هذه الجلسة:** ربط ناتج الجلسة السابقة (`baghdad_routing.db`، 361,448
عقدة / 772,755 حافة) بالتطبيق فعلياً - كأصل Flutter (asset) يُنسخ إلى
`routing_nodes`/`routing_edges` الموجودتين أصلاً (فارغتين منذ الجلسة
الأولى) داخل `iraq_cycling.db` عند أول تشغيل. **لا** منطق Dijkstra/A*
بعد.

**قرار معماري طُرح على المستخدم صراحة قبل التنفيذ** (تضارب تقني حقيقي:
الجدولان فارغان أصلاً داخل الـsingle shared bootstrap، فهل يصبح
الأصل ملفاً منفصلاً أم يُغذّي نفس الجدولين؟): المستخدم اختار **التغذية
داخل الجدولين الموجودين** (الخيار المتوافق مع نمط "كل الجداول في
bootstrap واحد" الموثق أعلاه في هذا الملف) بدل ملف db مستقل.

**التنفيذ:**
- `assets/routing/baghdad_routing.db` (نسخة من ناتج الجلسة السابقة،
  ~89 ميجابايت) + تسجيله في `pubspec.yaml` تحت `flutter: assets:`.
- `lib/services/routing_graph_seeder.dart` (`RoutingGraphSeeder`
  class جديد): `seedIfNeeded(db)` تتحقق أولاً من `COUNT(*)` على كلا
  الجدولين؛ إن كان أيّهما فارغاً (تشغيل أول، أو seed سابق توقف بمنتصفه)
  تُحمّل الأصل عبر `rootBundle`، تكتبه لملف مؤقت (`path_provider`
  `getTemporaryDirectory`)، `ATTACH DATABASE` له (خارج أي transaction -
  SQLite يرفض ATTACH داخل transaction مفتوحة)، ثم داخل `db.transaction`
  واحدة: حذف كامل لـ`routing_edges` ثم `routing_nodes` (بنفس ترتيب
  إعادة الاستيراد الموثق سابقاً)، ثم `INSERT ... SELECT` من قاعدة
  الـseed المرفقة لكلا الجدولين (routing_nodes أولاً). `DETACH` بعدها،
  وحذف الملف المؤقت. النسخ والإدراج ينفذان بالكامل داخل محرك SQLite
  (لا مرور عبر platform channel لكل صف) - ضروري لحجم ~1.1 مليون صف.
- `lib/main.dart`: يفتح `openAppDatabase()` صراحة عند بدء التطبيق
  ويستدعي `RoutingGraphSeeder().seedIfNeeded(appDatabase)` قبل
  `runApp` (بنفس نمط `mapTileService.init()`/`userProfileRepository.load()`
  الموجود مسبقاً).
- `test/routing_graph_seeder_test.dart` (جديد): يبني seed db صغيراً
  وهمياً (عقدتان/حافة واحدة) عبر `sqflite_common_ffi`، ويستخدم
  `_FakeAssetBundle` (يمدّد `CachingAssetBundle`) لتفادي الحاجة لمحرك
  Flutter الحقيقي في الاختبار. يغطي: النسخ الأولي الناجح، عدم إعادة
  التحميل إن كانت البيانات موجودة أصلاً (`loadCount == 1` رغم استدعاء
  `seedIfNeeded` مرتين)، والتعافي الذاتي من seed سابق متوقف بمنتصفه
  (عقدة واحدة يتيمة موجودة، لا حواف).

**اختبار فعلي على جهاز حقيقي (نفس RMX3941 عبر adb، بنفس منهجية جلسات
الاختبار اليدوي السابقة):**
- `flutter build apk --debug` نجح (APK ~241 ميجابايت، يتضمن أصل
  الـ89 ميجابايت). `flutter analyze`: "No issues found!". حزمة
  الاختبارات الكاملة (62 اختباراً) نجحت.
- تثبيت وتشغيل فعلي: `logcat` أظهر عمليتي "Long db operation" فعليتين
  (~7.8s و~8.9s) على `iraq_cycling.db` أثناء أول تشغيل - تطابق زمنياً
  عملية transaction الحذف/الإدراج، بدون أي `FATAL`/`AndroidRuntime`
  exception، والعملية بقيت حية طوال ذلك.
- تحقق مباشر بسحب ملف قاعدة البيانات من الجهاز (`run-as` + `cat`)
  وفحصه محلياً: `routing_nodes` = 361,448، `routing_edges` = 772,755 -
  مطابق تماماً لعدّ الأصل نفسه.
- إعادة تشغيل ثانية (بعد `force-stop`): لا "Long db operation" جديدة
  (seeding يُتخطّى كما هو متوقع)، لا crash - يثبت idempotency فعلياً
  على جهاز حقيقي وليس فقط في اختبارات FFI.

**git/الأصل الكبير - القرار النهائي:** `assets/routing/
baghdad_routing.db` (~89 ميجابايت) **غير** مضاف لـ`.gitignore` لأن
`pubspec.yaml` يشير إليه كأصل بناء فعلي (خلافاً لنسخة
`tool/routing_import/` التي تبقى متجاهَلة). طُرح على المستخدم صراحة
قبل أي commit: الخيار الأول (موصى به) كان Git LFS - لكن `git-lfs` غير
مثبت على هذه البيئة، ومحاولة `sudo pacman -S git-lfs` لم تنجح (الحزمة
بقيت غير مثبتة بعد التنفيذ، لا سبب واضح ظهر في وقت الجلسة). بعد ذلك
اختار المستخدم صراحة الالتزام (`git commit`) بالملف مباشرة كما هو، رغم
حجمه - **مقبول بقرار صريح من المستخدم في هذه الجلسة**، وليس افتراضاً
ضمنياً.

**الخطوة التالية (لأي جلسة قادمة):**
1. إن تكرر استيراد OSM لاحقاً (بيانات محدّثة)، تذكّر أن كل إعادة
   استيراد ستضيف ~89 ميجابايت جديدة لتاريخ git (الملف يُستبدل بالكامل
   في كل مرة، لا diff تدريجي) - قد يستحق مراجعة Git LFS لاحقاً إن أصبح
   متاحاً على بيئة أخرى.
2. جدول مُعاملات `weight` الفعلي - لا يزال مؤجلاً.
3. تنفيذ Dijkstra/A* الذي يقرأ من `routing_nodes`/`routing_edges` -
   لم يبدأ بعد.
4. قد يستحق قياس تأثير أصل الـ89 ميجابايت على حجم تنزيل APK/AAB
   للإصدار النهائي (release build غير debug) قبل الإطلاق.

---

### جلسة 2026-08-14 (الثانية) - تنفيذ فعلي لخط أنابيب استيراد بيانات OSM (nodes/edges مملوءة، بدون pathfinding)

**نطاق هذه الجلسة بدقة:** تنفيذ خط الأنابيب الموصوف في الجلسة السابقة
(أدناه) فعلياً - سحب بيانات OSM حقيقية، تحويلها، وملء قاعدة بيانات
sqlite منفصلة بنفس مخطط `routing_nodes`/`routing_edges`. **لا** منطق
Dijkstra/A* في هذه الجلسة، و**لا** دمج للناتج داخل أصول التطبيق
(assets) بعد - ملف قاعدة البيانات الناتج يعيش حالياً فقط في
`tool/routing_import/` محلياً، غير مُشحَّن.

**الأداة:** `tool/routing_import/build_routing_graph.py` - سكربت Python
مستقل (build-time، لا يعمل على الجهاز)، يستخدم `sqlite3` و`urllib` من
المكتبة القياسية فقط (لا `osmium` مثبت على هذه البيئة، فاختيرت طريقة
Overpass API المباشرة الموثقة كخيار 1 أدناه بدل مقتطف `.osm.pbf`).
يقبل إما `--fetch` (استعلام Overpass مباشرة) أو `-i <ملف json محفوظ
مسبقاً>`، ويكتب db بمخطط مطابق حرفياً لـ`_createRoutingGraphTables` في
`app_database.dart` (نفس DDL، نفس الفهارس الثلاثة، نفس FK
`ON DELETE CASCADE`، `PRAGMA foreign_keys = ON`).

**مصدر البيانات:** استعلام Overpass API الموثق أدناه بالضبط، بصندوق
الحدود `33.0,44.1,33.6,44.6` (مطابق لـ`BaghdadRegion`). الاستعلام الأول
(بعد إعادة محاولتين بسبب "server too busy" من الخادم العام
overpass-api.de) رجع 84,005 way و366,176 node خام (~85 ميجابايت JSON).

**ملاحظة مهمة اكتُشفت أثناء التنفيذ ومعالجتها:** فلتر `[bbox:...]` في
Overpass يختار أي way **يتقاطع** مع الصندوق ثم يرجع كل عُقده كاملة غير
مقصوصة - فبعض العُقد (1.29% من الإجمالي، 4,721 عقدة) وقعت فعلياً خارج
33.0–33.6/44.1–44.6 (حتى 32.74–33.75 / 43.82–44.82)، غالباً من طرق
رئيسية تمتد خارج بغداد. تم إصلاح هذا بفلترة `node_coords` داخل السكربت
لقبول فقط العُقد ضمن BBOX بالضبط قبل بناء الحواف؛ أي حافة أحد طرفيها
خارج الصندوق تُسقط تلقائياً (لأن السكربت أصلاً يتجاهل أي segment طرفه
غير موجود في `node_coords`). بعد الفلترة: حدود `routing_nodes` الفعلية
(33.0000011–33.5999984 / 44.1000344–44.5999922) داخل الصندوق تماماً.

**قرارات التحويل الفعلية المطبّقة (تطابق التصميم الموثق في الجلسة
السابقة):**
- `distance_meters`: نفس صيغة Haversine ونفس `_earthRadiusMeters` = 6371000
  الموجودة في `lib/utils/distance_calculator.dart`، معاد تطبيقها بـ
  Python بنفس الثوابت (لا صيغة مسافة ثانية مختلفة).
- `weight` = `distance_meters` مبدئياً (جدول المُعاملات حسب نوع الطريق/
  السطح لا يزال قراراً منفصلاً مؤجلاً كما تقرر سابقاً).
- الاتجاهية: أولوية لـ`oneway:bicycle` على `oneway` العام. **ملاحظة من
  هذا الـsnapshot تحديداً:** لا way واحد في نتيجة الاستعلام يحمل
  `oneway:bicycle` (كل القيم None) - فكل الحواف اعتمدت فعلياً على
  `oneway` العام فقط (8,041 way بـ`oneway=yes`، 136 بـ`oneway=no`،
  الباقي بلا وسم = ثنائي الاتجاه). هذا سلوك متوقع من بيانات OSM
  الحالية لبغداد وليس خطأً بالسكربت - لكن يعني عملياً أن مسار contraflow
  للدراجات على شوارع أحادية الاتجاه غير موجود بعد في بيانات OSM نفسها،
  فلن يظهر في الرسم البياني حتى يُضاف الوسم في OSM لاحقاً.
- `cycleway`/`surface`: **لا way واحد في هذا الـsnapshot يحمل أي وسم
  `cycleway*`**، فعمود `cycleway` فارغ (NULL) في كل الصفوف حالياً. هذا
  مهم لمن يصمم جدول مُعاملات `weight` لاحقاً: لا توجد إشارة "ممر دراجات
  مخصص" في البيانات الحالية للاعتماد عليها - أي تفضيل لممرات الدراجات
  سيحتاج مصدر بيانات إضافي أو انتظار تحسّن وسم OSM لبغداد.

**التحقق المُنفَّذ (يطابق البند 10 من خط الأنابيب الموثق):**
- الفهارس والـDDL في `baghdad_routing.db` مطابقة حرفياً لما في
  `app_database.dart` (تحقق بمقارنة `sqlite_master` مباشرة).
- 0 حافة يتيمة (`LEFT JOIN` من `routing_edges` على كلا الطرفين في
  `routing_nodes`، بحث صريح عن `NULL`).
- فحص يدوي: عُقد بالقرب من ساحة التحرير (33.34N/44.40E تقريباً) موجودة
  فعلاً في الناتج.

**النتيجة النهائية:**
- `routing_nodes`: **361,448** صفاً
- `routing_edges`: **772,755** صفاً
- حجم الملف: `tool/routing_import/baghdad_routing.db` = **93,278,208
  بايت (~88.96 ميجابايت)**
- ملف JSON الخام من Overpass (`baghdad_osm_raw.json`) محفوظ محلياً أيضاً
  (~81 ميجابايت) كذاكرة تخزين مؤقت تسمح بإعادة تشغيل السكربت دون ضرب
  Overpass API مرة أخرى.

**git:** لم يُنفَّذ أي `git commit`/`push`. أُضيف
`tool/routing_import/*.json` و`tool/routing_import/*.db` إلى
`.gitignore` (الملفان الناتجان كبيران جداً وقابلان لإعادة التوليد -
السكربت نفسه هو المصدر الذي يجب تتبعه بـgit، وليس ناتجه). حجم كل من
الملفين أُبلغ للمستخدم صراحة قبل أي طرح لسؤال commit.

**الخطوة التالية (لأي جلسة قادمة):**
1. قرار الشحن: كيف يُنقل `baghdad_routing.db` (أو نسخة منه) إلى
   `assets/` الخاصة بـFlutter ويُنسخ لموقعه عند أول تشغيل (بنفس روح
   `MapTileService` لكن كـbuild-time asset) - لم يُنفَّذ بعد.
2. جدول مُعاملات `weight` الفعلي (يحتاج رأي دراج حقيقي، ليس أرقاماً
   مُختلقة) - لا يزال مؤجلاً عمداً.
3. تنفيذ Dijkstra/A* الذي يقرأ من هذين الجدولين - لم يبدأ.
4. **لم يُطلب في نطاق هذه الجلسة**: تبسيط عُقد التمرير ذات الدرجة 2
   (دمج سلاسل غير متقاطعة لحافة واحدة) - الحجم الحالي (~361K عقدة)
   يبقى قابلاً للتعامل به الآن لكن قد يستحق مراجعة أداء لاحقاً.

---

### جلسة 2026-08-14 - مخطط قاعدة بيانات التوجيه (routing graph) + توثيق خط الأنابيب (schema فقط، بدون استيراد فعلي أو pathfinding)

**نطاق هذه الجلسة بدقة (لا تتجاوزه أي جلسة قادمة بدون طلب صريح):**
1. تصميم وإضافة migration لجداول nodes/edges لرسم بياني توجيه دراجات
   offline، محصور بمنطقة بغداد نفسها المستخدمة فعلاً في `BaghdadRegion`
   (map_tile_service.dart): 33.0–33.6 شمالاً / 44.1–44.6 شرقاً.
2. توثيق خطوات استخراج بيانات OSM قابلة للتوجيه (highway=*, cycleway=*,
   oneway, surface) وتحويلها لهذا الـschema (هذا القسم أدناه).
3. **لم يتم تحميل أو معالجة أي OSM extract فعلي في هذه الجلسة.** لا
   سكربت تحويل فعلي، لا بيانات مستوردة، لا جداول مملوءة.
4. **لم يتم تنفيذ أي منطق Dijkstra/A* أو أي قراءة توجيه فعلية.** هذه
   الجداول فارغة تماماً حتى تُبنى خط الأنابيب الموصوف أدناه فعلياً في
   جلسة لاحقة منفصلة.

**التغيير الفعلي في الكود:**
- `lib/services/app_database.dart`: رفع `appDbVersion` من 4 إلى 5،
  وإضافة `_createRoutingGraphTables` (تُستدعى من `onCreate` ومن فرع
  `oldVersion < 5` في `onUpgrade`، بنفس نمط الترقيات السابقة في هذا
  الملف). التوثيق الكامل لتصميم الجدولين موجود كتعليق دوك مباشرة فوق
  `_createRoutingGraphTables` في الكود نفسه (سبب اختيار `id` = معرّف
  عقدة OSM نفسه بدل autoincrement، ولماذا الحواف directed بدل تخزين
  علم "ثنائي الاتجاه").
- `test/app_database_test.dart` (ملف جديد): تحقق من شكل الجدولين
  (الأعمدة، الفهارس الثلاثة على `routing_edges`، قبول `routing_nodes`
  لقيمة `id` صريحة، وسلوك `ON DELETE CASCADE` من node إلى الحواف
  المرتبطة به).

**مخطط الجداول (routing_nodes / routing_edges):**

```
CREATE TABLE routing_nodes (
  id INTEGER PRIMARY KEY,      -- نفس معرّف عقدة OSM (node id)، وليس autoincrement
  latitude REAL NOT NULL,
  longitude REAL NOT NULL
);

CREATE TABLE routing_edges (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  from_node_id INTEGER NOT NULL REFERENCES routing_nodes(id) ON DELETE CASCADE,
  to_node_id   INTEGER NOT NULL REFERENCES routing_nodes(id) ON DELETE CASCADE,
  osm_way_id   INTEGER NOT NULL,   -- الـway المصدر، للتصحيح وإعادة الاستيراد
  highway_type TEXT NOT NULL,      -- قيمة وسم highway=* كما هي من OSM
  cycleway     TEXT,               -- قيمة وسم cycleway=* إن وُجدت
  surface      TEXT,               -- قيمة وسم surface=* إن وُجدت
  oneway       INTEGER NOT NULL DEFAULT 0,  -- 0/1، للتوثيق فقط (كل صف أصلاً باتجاه واحد)
  distance_meters REAL NOT NULL,   -- المسافة الجغرافية الفعلية بين العقدتين
  weight       REAL NOT NULL       -- تكلفة التوجيه الفعلية (قد تختلف عن المسافة، انظر أدناه)
);
CREATE INDEX idx_routing_edges_from_node ON routing_edges (from_node_id);
CREATE INDEX idx_routing_edges_to_node   ON routing_edges (to_node_id);
CREATE INDEX idx_routing_edges_osm_way   ON routing_edges (osm_way_id);
```

قرار تصميم مهم: كل صف في `routing_edges` هو حافة **باتجاه واحد**
(directed). طريق OSM بدون `oneway` (أو `oneway=no`) يُنتج صفّين
(اتجاهين)؛ `oneway=yes` يُنتج صفاً واحداً بترتيب عقد الـway كما هو؛
`oneway=-1` يُنتج صفاً واحداً بترتيب معكوس. الهدف: أي خوارزمية Dijkstra/
A* مستقبلية تمشي فقط على `WHERE from_node_id = ?` بدون أي فرع خاص
بـ oneway وقت التنفيذ - كل التعقيد يُحل مرة واحدة وقت الاستيراد.

**خط أنابيب بيانات التوجيه (OSM → nodes/edges) - موثّق فقط، غير منفَّذ بعد:**

1. **مصدر البيانات:** بدل تحميل مقتطف عراق كامل من Geofabrik (يحتاج
   قص/clip لاحقاً)، الأنسب لمنطقة صغيرة كبغداد هو استعلام Overpass API
   مباشرة بصندوق حدودي (bbox) مطابق تماماً لـ`BaghdadRegion`:
   ```
   [bbox:33.0,44.1,33.6,44.6];
   (
     way["highway"];
     way["cycleway"];
   );
   out body geom;
   >;
   out skel qt;
   ```
   `out geom` يُرجع إحداثيات كل عقدة مباشرة مع كل way، فيوفّر استعلاماً
   منفصلاً لكل عقدة.

2. **إن استُخدم مقتطف .osm.pbf بدل Overpass** (مثلاً لو احتجنا سجلاً
   محلياً قابلاً لإعادة التشغيل دون الاعتماد على Overpass كل مرة):
   `osmium extract` بصندوق الحدود نفسه، ثم `osmium tags-filter
   w/highway w/cycleway` لإسقاط كل شيء غير قابل للتوجيه (مبانٍ،
   landuse، إلخ) مع الاحتفاظ بكل العُقد المرجعية لتلك الـways.

3. **فلترة الوسوم القابلة للتوجيه فعلياً:** الاحتفاظ فقط بـ ways تحمل
   `highway=*` أو `cycleway=*`. داخل كل way محتفظ به، تُقرأ الوسوم:
   `highway`, `cycleway` (وأيضاً `cycleway:left`/`cycleway:right` إن
   وُجدت - OSM يفصلها أحياناً حسب جانب الطريق)، `oneway`، و**تحديداً
   `oneway:bicycle`** قبل `oneway` العام - شارع باتجاه واحد للسيارات
   قد يكون ثنائي الاتجاه للدراجات (contraflow cycling)، وهذا التطبيق
   مختص بالدراجات فقط فلا يجوز تجاهل هذا الوسم، و`surface`.

4. **بناء العُقد (nodes):** في هذا الإصدار الأول من خط الأنابيب، كل
   عقدة OSM يشير إليها أي way محتفظ به تصبح صفاً في `routing_nodes`
   مباشرة (بدون تبسيط/دمج عُقد التمرير ذات الدرجة 2 التي ليست
   تقاطعات). هذا أبسط ويكفي لحجم بغداد، لكنه سيُنتج رسماً بيانياً أكبر
   من اللازم - تبسيط لاحق (دمج سلاسل العُقد المتتالية غير المتقاطعة
   إلى حافة واحدة بهندسة مركّبة) متروك لمرحلة لاحقة إن ثبت أن الحجم
   يشكّل مشكلة أداء فعلية.

5. **بناء الحواف (edges) من تسلسل عُقد كل way:** لكل زوج عُقد متتاليين
   `(n[i], n[i+1])` في الـway، تُنشأ حافة أو حافتان حسب `oneway`/
   `oneway:bicycle` كما في قرار التصميم أعلاه.

6. **حساب `distance_meters`:** المسافة الجغرافية الفعلية (great-circle)
   بين إحداثيي العقدتين. **يجب استخدام نفس صيغة Haversine الموجودة
   فعلاً في `lib/utils/distance_calculator.dart`** بدل إعادة تطبيقها
   بلغة/سكربت منفصل - لتفادي وجود صيغتي مسافة مختلفتين في نفس التطبيق
   قد تُنتجان أرقاماً متعارضة لنفس الإحداثيات.

7. **حساب `weight`:** تكلفة التوجيه الفعلية، تبدأ من `distance_meters`
   وتُضرب بمُعامل حسب وسوم الحافة (نوع الطريق، وجود `cycleway`
   مخصصة، نوعية `surface`) بحيث تُفضَّل ممرات الدراجات المخصصة والسطح
   المعبّد على طرق سريعة/غير معبّدة. **جدول المُعاملات نفسه قرار
   منفصل** يحتاج رأي دراج حقيقي بالمنطقة وليس أرقاماً مُختلقة الآن -
   خارج نطاق هذه الجلسة عمداً. إلى حين تحديده، `weight` يمكن أن يساوي
   `distance_meters` كقيمة ابتدائية محايدة.

8. **الكتابة إلى sqflite:** إدراج جماعي (batch insert) داخل
   `db.transaction` واحدة - `routing_nodes` أولاً ثم `routing_edges`
   (بسبب الـFOREIGN KEY). إعادة استيراد لاحقة (بيانات OSM محدّثة):
   حذف كامل لكل الصفوف (`routing_edges` أولاً بسبب الـFK) ثم إعادة
   إدراج من الصفر - لا يوجد تخطيط لـdiff/upsert تدريجي في هذا الإصدار.

9. **أين يعيش خط الأنابيب فعلياً:** أداة مطوّر تعمل مرة واحدة (offline
   build-time tool) - **ليست** كوداً يعمل داخل التطبيق على الهاتف. لا
   يجوز تحليل .osm.pbf أو تشغيل osmium على الجهاز نفسه. الناتج
   (routing_nodes/routing_edges جاهزة) يُشحن كملف قاعدة بيانات مُعبّأ
   مسبقاً (asset) يُنسخ لمكانه عند أول تشغيل - بنفس روح تحميل بلاطات
   MapTileService لكن كـ build-time asset بدل تحميل من المستخدم، فلا
   تنطبق هنا نفس هواجس سياسة استخدام OSM tile server المذكورة في
   map_tile_service.dart (Overpass API له سياسة استخدام مختلفة، ولأن
   هذا استخراج لمرة واحدة أثناء التطوير وليس تحميلاً متكرراً من كل
   مستخدم نهائي). أداة التنفيذ الفعلية (سكربت Dart/Python + osmium، أو
   Overpass مباشرة) قرار مفتوح متروك لجلسة التنفيذ.

10. **تحقق قبل شحن أي بيانات مُولَّدة مستقبلاً:** عدّ الصفوف مقابل
    توقع معقول لحجم شبكة طرق بغداد، التأكد من عدم وجود حواف يتيمة
    (مضمون أصلاً عبر الـFK طالما `PRAGMA foreign_keys = ON` مفعّلة كما
    هي فعلاً في `openAppDatabase`)، وفحص يدوي لعينة من تقاطعات بغداد
    معروفة.

**flutter analyze:** نجح - "No issues found!"

**ملاحظة git:** لم يتم تنفيذ أي git commit أو push في هذه الجلسة، حسب
تعليمات المهمة الصريحة.

**الخطوة التالية (لأي جلسة قادمة):** تنفيذ خط الأنابيب الموصوف أعلاه
فعلياً (سكربت تحويل + استيراد فعلي لمنطقة بغداد)، ثم لاحقاً - وفي
جلسة منفصلة تماماً - تنفيذ خوارزمية Dijkstra/A* التي تقرأ من هذين
الجدولين. لا شيء من هذين الاثنين بدأ بعد.

---

### جلسة 2026-07-11 (الخامسة) - اختبار جلسة "إضافة الجنس + العرض الحي" على جهاز حقيقي

**الوقت:** 2026-07-11، جلسة منفصلة مخصصة لاختبار جلسة "إضافة الجنس لحساب
السعرات + عرض حي أثناء الرحلة" (أدناه) على جهاز حقيقي، بنفس منهجية
جلسات الاختبار اليدوي السابقة (adb + uiautomator/screencap + فحص logcat
مفلتر بـ PID).

**الجهاز المستخدم:** نفس RMX3941 (Realme) عبر USB/adb.

**ما تم التحقق منه فعلياً:**

1. **الترقية (migration) على قاعدة بيانات حقيقية موجودة مسبقاً:** تم
   بناء وتثبيت APK جديد يحتوي على ترقية `appDbVersion` من 3 إلى 4 (عمود
   `gender` جديد)، فوق قاعدة بيانات الجهاز الحقيقية التي تحتوي فعلياً
   على صف ملف شخصي محفوظ من جلسات سابقة (الاسم "أسعد القريشي"، وزن 79،
   عمر 40). بعد الإطلاق: لا crash، ولا استثناءات في logcat. تم سحب ملف
   قاعدة البيانات فعلياً عبر `run-as` + فحصه بـ sqlite3 محلياً، وتأكّد
   أن الصف الموجود مسبقاً حصل تلقائياً على `gender='male'` (القيمة
   الافتراضية `DEFAULT 'male'` في الـ`ALTER TABLE`) دون أي فقدان بيانات
   أو تلف في الأعمدة الأخرى.

2. **مسار القراءة (load) للجنس - تم التحقق منه بشكل حاسم:** بما أن
   التفاعل التلقائي عبر adb مع SegmentedButton الجنس واجه مشكلة توقيت
   حقيقية (انظر أدناه)، تم استخدام طريقة بديلة أكثر موثوقية: تعديل قيمة
   `gender` مباشرة إلى `'female'` في ملف قاعدة البيانات على الجهاز (سحب
   الملف عبر `run-as cat`، تعديله محلياً بـ sqlite3، إعادة رفعه)، ثم
   إعادة تشغيل التطبيق بالكامل من الصفر وفتح شاشة الملف الشخصي. **النتيجة:
   ظهرت "أنثى" محددة بشكل صحيح فوراً** - يثبت أن مسار
   `UserProfile.fromMap` → `UserProfileRepository.load` →
   `ProfileScreen.initState` يعمل بشكل صحيح فعلياً على جهاز حقيقي بصف
   قاعدة بيانات حقيقي، وليس فقط في اختبارات الوحدة.

3. **آلية الحفظ تعمل:** تم التحقق عدة مرات أن الضغط على "حفظ" يُظهر
   Snackbar "تم حفظ الملف الشخصي" فوراً ويكتب فعلياً إلى القرص (تم
   التحقق عبر سحب قاعدة البيانات بعد الحفظ).

4. **عمود "السعرات الحرارية" الحي:** يظهر بشكل صحيح على شاشة التتبع
   بجانب "نبض القلب" و"ارتفاع التسلق"، بنفس نمط `StatColumn` الموجود،
   بعد استقرار الشاشة الأولى (شوهد فارغاً من التسميات في أول لقطة شاشة
   مباشرة بعد الإطلاق البارد - تبيّن أنه مجرد أثر لقطة شاشة مبكرة قبل
   اكتمال أول رسم كامل، وليس عيباً حقيقياً؛ تكرر التحقق بعد فترة انتظار
   كافية وظهرت التسميات بشكل صحيح دائماً).

**مشكلة حقيقية واجهتها منهجية الاختبار الآلي (adb) - وليست عيباً في
الكود:** محاولات تكرارية لتحديد إحداثيات دقيقة لزر "أنثى" ثم "حفظ" عبر
`adb shell input tap` بالتتابع السريع أدت لنتائج غير متسقة (أحياناً
يظهر التبديل صحيحاً بصرياً في لقطة شاشة، لكن القيمة المحفوظة فعلياً في
قاعدة البيانات تكون القيمة القديمة). تم تتبّع السبب إلى انزياح فعلي في
موضع التمرير (scroll position) لشاشة الملف الشخصي بين لحظة التقاط لقطة
الشاشة ولحظة تنفيذ اللمسة الفعلية على الجهاز - **تأكّد حدوث هذا الانزياح
حتى بدون أي تدخل مني (بين لقطتين متتاليتين بدون أي لمسة بينهما)**، مما
يستبعد أن يكون السبب لمساتي نفسها. تمت مراجعة كود `profile_screen.dart`
بعناية (حقل `_gender` هو حقل State عادي، يُقرأ بشكل متزامن في `_save()`،
بنفس نمط `_preferredUnit` الموجود مسبقاً والذي **لم** يُظهر هذه المشكلة)
- **لا يوجد أي خلل في الكود نفسه**؛ هذا افتراضياً أثر توقيت خاص بلمسات
adb الآلية السريعة على هذا الجهاز تحديداً (على الأرجح مرتبط بتغييرات
`viewInsets`/تركيز لوحة المفاتيح عند اقتراب اللمسة من حقول النص أعلى
قسم الجنس)، وليس شيئاً سيواجهه مستخدم حقيقي يلمس الشاشة بالسرعة
الطبيعية ويرى كل تغيير قبل التالي. **لم يُصلَح لأنه ليس عيباً في
التطبيق - موثّق هنا فقط كملاحظة منهجية اختبار لأي جلسة آلية مستقبلية.**

**تنظيف:** تمت إعادة قيمة `gender` في قاعدة بيانات الجهاز إلى `'male'`
الأصلية (ووزن 79/عمر 40/وحدة كم كما كانت) قبل إنهاء الجلسة - لا بيانات
اختبار متبقية على الجهاز.

**لم يُختبر في هذه الجلسة:** اقتران فعلي بجهاز BLE حقيقي (Fitbit Charge
6) لمشاهدة `liveCalories` يتراكم حياً - يحتاج حل مشكلة GATT status=147
الموثّقة سابقاً أولاً.

**نتيجة logcat:** لا استثناءات أو crashes خاصة بعملية التطبيق طوال
الجلسة (تم الفلترة بـ PID عبر عدة إعادة إطلاق).

**Git:** لم يُنفَّذ أي git commit أو push.

---

### جلسة 2026-07-11 (الرابعة) - إضافة الجنس لحساب السعرات + عرض حي أثناء الرحلة

**الوقت:** 2026-07-11، جلسة واحدة، فوق Phase 1/1.5/2/2.5/3 القائمة.

**ملاحظة عن مصدر المهمة:** طُلبت هذه الجلسة عبر تعليمات خارجية طلبت بناء
"CaloriesEngine" جديد بالكامل (كائن `UserProfile` جديد، `CalorieSample`،
منطق Keytel من الصفر) بافتراض أن لا شيء من هذا موجود بعد. **توقفت قبل
كتابة أي كود** (حسب تعليمة التعليمات نفسها بالتوقف عند تعارض) بعد أن
كشف الفحص الفعلي للكود أن:
- `lib/utils/calorie_calculator.dart` يطبّق بالفعل نفس صيغة Keytel
  بالضبط (المعاملات مطابقة حرفياً)، ومربوط فعلياً بـ `RideTracker`
  ومختبَر على جهاز حقيقي (انظر جلسة Phase 2.5 الثانية أعلاه).
- `lib/models/user_profile.dart` + `lib/services/user_profile_repository.dart`
  موجودان فعلاً (تخزين sqflite، ليس GetStorage/SharedPreferences).
- تدفق نبض القلب من BLE (`HeartRateService.bpmStream`) مربوط فعلياً
  بحساب السعرات في `RideTracker.finishRide()`.
- الفجوة الحقيقية الوحيدة: **لا حقل `gender`** في `UserProfile` (الصيغة
  تفترض دائماً `Gender.male`)، ولا **عرض حي** للسعرات أثناء الرحلة
  (الحساب الحالي يتم مرة واحدة فقط عند إنهاء الرحلة من متوسط النبض)،
  ولا حماية من قفزات BLE (إعادة اتصال) في أي عرض حي.
- كذلك، وصف التعليمات لسمات الواجهة (لون #00D4AA، خلفية #060A0F، خط
  Cairo) لا ينطبق على هذا التطبيق إطلاقاً (ذلك من مشروع مختلف تماماً)
  - نمط الواجهة الفعلي هنا أسود/أخضر قياسي، فاتُّبع النمط الفعلي بدل
    المُدخَل الخاطئ في التعليمات.

**قرار المطوّر بعد عرض الخيارات:** تعديل بسيط (minimal diff) فوق الكود
القائم بدل بناء `CaloriesEngine` موازٍ جديد - إضافة `gender` فقط،
وإضافة عرض حي، دون تكرار صيغة الحساب.

**الملفات المعدّلة:**
- `lib/utils/calorie_calculator.dart` - استُخرجت الصيغة الأساسية لكل
  دقيقة إلى دالة نقية جديدة `caloriesPerMinuteFromHeartRate()` (يستدعيها
  الآن `estimateCaloriesBurned()` أيضاً بدل تكرار الصيغة). أُضيفت
  `resolveGender()` (نفس نمط resolveWeightKg/resolveAgeYears)، وصنف
  جديد `LiveCalorieAccumulator` يتراكم السعرات الحية عبر عيّنات نبض
  متتالية: يرفض قراءات خارج 40-220 نبضة/دقيقة (خلل مستشعر)، ويحدّ
  الفارق الزمني بين عيّنتين بحد أقصى دقيقتين (حماية من احتساب فجوة
  إعادة اتصال BLE كاملة - نفس مشكلة status=147 الموثّقة في جلسة Phase 2
  أعلاه - كسعرات فعلية).
- `lib/models/user_profile.dart` - إضافة حقل `gender` (يستخدم `Gender`
  enum الموجود أصلاً في calorie_calculator.dart بدل تكراره)، افتراضي
  `Gender.male` (يطابق سلوك ما قبل هذا الحقل).
- `lib/services/app_database.dart` - رفع `appDbVersion` من 3 إلى 4،
  عمود `gender` جديد في `user_profile` (ضمن CREATE TABLE للتركيبات
  الجديدة، و`ALTER TABLE` للترقية من نسخة 2/3 فقط - مع حارس صريح
  لتفادي تكرار إضافة العمود إن كانت الترقية من نسخة أقدم من 2، حيث
  الجدول يُنشأ حديثاً في نفس onUpgrade بالعمود موجوداً أصلاً).
- `lib/services/ride_tracker.dart` - `finishRide()` يمرر الآن
  `resolveGender(profile?.gender)` لصيغة السعرات بدل الاعتماد على
  `Gender.male` الافتراضي الصامت. أُضيف `LiveCalorieAccumulator` جديد
  يُنشأ في `startRide()` بقيم الملف الشخصي، يُغذّى من كل عيّنة `bpmStream`
  في `_onBpm()` (بجانب متوسط النبض الحالي، دون تعديله)، ومتاح عبر
  `liveCalories` getter جديد. `_onBpm()` يستدعي الآن `notifyListeners()`
  ليحدّث العرض الحي فوراً مع كل نبضة، وليس فقط مع كل نقطة GPS.
- `lib/screens/profile_screen.dart` - إضافة `SegmentedButton<Gender>`
  (ذكر/أنثى) بنفس نمط اختيار وحدة القياس الموجود تماماً.
- `lib/screens/tracking_screen.dart` - إضافة `StatColumn` خامسة
  "السعرات الحرارية" تعرض `tracker.liveCalories` حياً، بجانب صف نبض
  القلب.
- `test/calorie_calculator_test.dart` - 12 اختباراً جديداً:
  `resolveGender` (تمرير قيمة، سقوط لذكر)، `caloriesPerMinuteFromHeartRate`
  (تطابق الصيغة الموثّقة لكلا الجنسين، سقوط سالب لصفر)، و`LiveCalorieAccumulator`
  (صفر قبل أي عيّنة، عيّنة أولى وحيدة لا تُحسب، تراكم صحيح بين عيّنتين،
  رفض قراءة أعلى/أقل من 40-220 دون احتساب فجوتها، تحديد الفجوة بحد
  أقصى دقيقتين عند فجوة 10 دقائق، `reset()`).
- `test/user_profile_repository_test.dart` - توسيع اختبارين قائمين
  ليغطيا `gender` (القيمة الافتراضية، وحفظ/تحميل قيمة أنثى فعلياً عبر
  sqflite).

**لم يُعدَّل عمداً (خارج نطاق "الحد الأدنى من التعديل" الذي اختاره
المطوّر):**
- لا `sessionSummary()`/تتبع "أعلى نبض قلب لحظي" - هذا امتداد نطاق
  سبق أن قرر المطوّر تأجيله صراحة في جلسة Phase 2.5 (انظر أعلاه)، ولم
  يُعَد فتحه هنا.
- لم يُغيَّر حساب `Ride.caloriesBurned` المحفوظ (لا يزال من متوسط النبض
  عند إنهاء الرحلة فقط، كما كان، ومختبَراً على جهاز حقيقي من قبل) -
  العرض الحي الجديد منفصل تماماً وإضافي فقط.

**نتيجة flutter analyze:** نجح - "No issues found!"

**نتيجة flutter test:** نجحت جميعها (52 اختباراً؛ 41 موروثاً + 12 جديداً
في calorie_calculator_test.dart، مع توسيع اختبارين قائمين في
user_profile_repository_test.dart بدل إضافة اختبارين منفصلين - العدد
الصافي الجديد في ملفات الاختبار = 12).

**لم يُختبر على جهاز حقيقي في هذه الجلسة** - التحقق اقتصر على flutter
analyze/test ومراجعة الكود. يُوصى باختبار شاشة الملف الشخصي (حفظ قيمة
الجنس والتأكد من ثباتها عبر إعادة تشغيل) والعرض الحي للسعرات أثناء
رحلة فعلية مع حزام نبض متصل على جهاز حقيقي قبل الاعتماد الكامل.

**Git:** لم يُنفَّذ أي git commit أو push، حسب التعليمات الصريحة.

**الخطوة التالية:** اختبار هذه الجلسة على جهاز حقيقي، أو المتابعة على
أحد المسارات المفتوحة سابقاً (حل GATT status=147، أو تفعيل وحدة
القياس كم/ميل في شاشات العرض، أو Phase 4).

---

### جلسة 2026-07-11 (الثالثة) - Phase 3: تحذيرات مخاطر مرورية صوتية

**الوقت:** 2026-07-11، جلسة واحدة، تبني فوق Phase 1/1.5/2/2.5 القائمة.

**غموض تقني حقيقي عولج قبل البدء (حسب القاعدة الثابتة أعلاه):**
"Phase 3: تحذيرات مخاطر مرورية صوتية" في هذا الملف لم يكن يحتوي على
أي تفصيل عن مصدر "الخطر" أو آلية الصوت - فقط اسم المرحلة. توقفت وطرحت
سؤالين على المطوّر قبل كتابة أي كود:
1. **مصدر التحذير:** تم اختيار "قائمة نقاط مُنسَّقة يحتفظ بها المطوّر
   بنفسه" (بدل التحذير المعتمد على السرعة، أو استخراج تلقائي من بيانات
   OSM الوسمية التي كانت ستتطلب مصدر بيانات vector كامل جديد غير
   موجود حالياً - الكاش الحالي بلاطات PNG نقطية بلا وسوم دلالية).
2. **آلية الصوت:** تم اختيار Text-to-Speech (نطق جملة عربية كاملة) بدل
   صوت تنبيه بسيط (beep).

**قرار مهم إضافي لم يُطرح كسؤال بل اتُّخذ مباشرة (يستحق التوثيق):** لا
يحتوي هذا التطبيق على أي بيانات نقاط خطر مُدرجة مسبقاً (seeded/hardcoded)
لمدينة بغداد أو أي مكان آخر. **لا يوجد لدي أي مصدر موثوق لمواقع خطر
حقيقية ومتحقق منها في بغداد**، واختراع إحداثيات تبدو واقعية لميزة
تتعلق بالسلامة كان سيكون مضلِّلاً وخطيراً فعلياً لراكب حقيقي يعتمد على
هذه التحذيرات. بدل ذلك، بُنيت آلية كاملة لإضافة نقاط خطر حقيقية يُنشئها
المستخدم بنفسه بناءً على معرفته الفعلية بشوارع بغداد (زر "إضافة نقطة
خطر في موقعي الحالي" في شاشة إدارة نقاط الخطر). **قائمة نقاط الخطر
فارغة تماماً افتراضياً - يجب على المطوّر إضافة نقاطه الحقيقية بنفسه
قبل أن تكون هذه الميزة مفيدة فعلياً.**

**الملفات الجديدة:**
- `lib/models/traffic_hazard.dart` - `TrafficHazard` (id, latitude,
  longitude, radiusMeters افتراضي 50م, message) - تعليق صريح في الملف
  يوضح سبب عدم وجود بيانات مُدرجة مسبقاً (انظر أعلاه).
- `lib/services/hazard_repository.dart` - CRUD (getAll/add/delete) عبر
  sqflite المشترك من app_database.dart، بنفس نمط RideRepository/
  UserProfileRepository تماماً (بما في ذلك معامل `databasePath`
  الاختياري للاختبارات).
- `lib/utils/hazard_proximity.dart` - دالة نقية
  `newlyTriggeredHazardIds()`: تُرجع معرّفات نقاط الخطر التي دخل
  الموقع الحالي نطاقها ولم تُحذَّر بعد في هذه الرحلة (باستخدام
  Haversine الموجودة أصلاً في distance_calculator.dart). لا اعتماد
  على GPS أو قاعدة بيانات أو TTS إطلاقاً - قابلة للاختبار المباشر
  بإحداثيات عادية، بنفس فلسفة resolveWeightKg/resolveAgeYears من
  Phase 2.5.
- `lib/services/voice_alert_service.dart` - يغلّف flutter_tts، بنفس
  نمط `BleClient` من Phase 2 (واجهة `TtsClient` مستخرجة تُنفَّذ
  افتراضياً بـ `FlutterTtsClient` الحقيقية، لتمكين حقن كائن وهمي في
  الاختبارات وتفادي قناة platform channel حقيقية). لغة النطق تُضبَط
  على العربية (`ar`) عند الإنشاء.
- `lib/screens/hazard_management_screen.dart` - شاشة عربية: زر "إضافة
  نقطة خطر في موقعي الحالي" (يستخدم LocationService.getCurrentPosition()
  الموجودة، ثم حوار لإدخال رسالة التحذير ونطاق التنبيه بالمتر)، قائمة
  بنقاط الخطر المحفوظة مع إمكانية الحذف. رسالة واضحة عند القائمة
  الفارغة تشرح أن المستخدم يجب أن يضيف نقاطه الحقيقية بنفسه.
- `test/hazard_repository_test.dart` - 4 اختبارات (قائمة فارغة
  ابتدائياً، حفظ ثم تحميل من كائن جديد، تعيين id صحيح، الحذف يزيل
  السجل المستهدف فقط).
- `test/hazard_proximity_test.dart` - 5 اختبارات للدالة النقية
  (تحذير عند التطابق التام، عدم تحذير خارج النطاق، عدم تكرار التحذير
  لنفس النقطة، تجاهل نقاط بلا id محفوظ، تقييم عدة نقاط خطر بشكل
  مستقل).

**الملفات المعدّلة:**
- `pubspec.yaml` - إضافة `flutter_tts: ^4.2.5`. لا تعارضات تبعية.
- `lib/services/app_database.dart` - رفع `appDbVersion` من 2 إلى 3،
  إضافة جدول `traffic_hazards` جديد (id, latitude, longitude,
  radius_meters, message) مع مسار `onUpgrade` للتركيبات القديمة، دون
  المساس بالجداول الموجودة.
- `lib/services/ride_tracker.dart` - يقبل الآن `HazardRepository?` و
  `VoiceAlertService?` اختياريين (بنفس نمط الخدمات الاختيارية
  الأخرى). عند `startRide()`: تحميل كل نقاط الخطر مرة واحدة في الذاكرة
  ومسح قائمة "المُحذَّر منها" لهذه الرحلة. عند كل تحديث GPS
  (`_onPosition`): استدعاء `_checkHazards()` التي تستخدم
  `newlyTriggeredHazardIds()` وتُشغّل `voiceAlertService.announce()`
  (fire-and-forget، لا يُنتظر) لكل نقطة خطر جديدة، مع تسجيلها كمُحذَّر
  منها لتفادي تكرار نفس التحذير أثناء بقاء الراكب قرب نفس النقطة.
  `reset()` يمسح الحالة أيضاً.
- `lib/main.dart` - إنشاء `HazardRepository` و`VoiceAlertService`،
  تمريرهما لـ `RideTracker`، إضافة `HazardRepository` إلى
  `MultiProvider` (`VoiceAlertService` غير مطلوبة خارج RideTracker
  فلم تُضَف كـ Provider منفصل). `MyApp` يتطلب الآن معاملين إضافيين.
- `lib/screens/tracking_screen.dart` - إضافة أيقونة تحذير
  (`Icons.warning_amber_outlined`) خامسة في AppBar تفتح شاشة إدارة
  نقاط الخطر، بنفس نمط الأيقونات الموجودة.
- `test/widget_test.dart` - تمرير `HazardRepository()` طازجة (بدون
  استعلام فعلي، غير مُستخدَمة في مسار هذا الاختبار) و`VoiceAlertService`
  مع `_NoOpTtsClient` وهمي جديد (بنفس نمط `_NoOpBleClient`) لتفادي أي
  قناة platform channel حقيقية.

**نتيجة flutter analyze:** نجح - "No issues found!" (تم إصلاح تحذير
واحد `use_build_context_synchronously` في hazard_management_screen.dart
بإضافة فحص `mounted` بعد `await` حوار الإدخال).

**نتيجة flutter test:** نجحت 41/41 اختباراً (31 اختباراً موروثاً من
المراحل السابقة دون أي تغيير في نتيجتها + 4 اختبارات جديدة في
hazard_repository_test.dart + 5 اختبارات جديدة في hazard_proximity_test.dart
+ اختبار widget_test.dart المُحدَّث).

**لم يُختبر على جهاز حقيقي في هذه الجلسة** - التحقق اقتصر على flutter
analyze/test ومراجعة الكود. نقطتان تحديداً تستحقان اختباراً حقيقياً
قبل الاعتماد الكامل على الميزة:
1. أن `flutter_tts` ينطق فعلاً بصوت عربي مسموع على جهاز حقيقي (محرك
   TTS الافتراضي على الجهاز قد لا يحتوي صوتاً عربياً مثبَّتاً - في هذه
   الحالة `speak()` غالباً لن يُصدر أي صوت أو خطأ صامت بدل استثناء،
   حسب سلوك محرك أندرويد TTS القياسي).
2. أن آلية "إضافة نقطة خطر في موقعي الحالي" تعمل بشكل صحيح مع GPS
   حقيقي، وأن التحذير الصوتي فعلاً يُسمَع أثناء رحلة حقيقية عند
   الاقتراب من نقطة محفوظة.

**Git:** لم يُنفَّذ أي commit أو push.

**الخطوة التالية:** قرار المطوّر بين:
(أ) اختبار Phase 3 على جهاز حقيقي (النقطتان أعلاه تحديداً) قبل
الاعتماد عليها فعلياً أثناء ركوب حقيقي، بما في ذلك إضافة نقاط خطر
حقيقية من معرفة المطوّر الشخصية ببغداد (القائمة فارغة حالياً وستبقى
عديمة الفائدة حتى تُملأ ببيانات حقيقية).
(ب) البدء بـ Phase 4 (اشتراك مدفوع) وترك اختبار Phase 3 لجلسة لاحقة.
(ج) معالجة الديون التقنية المتراكمة من الجلسات السابقة (وحدة القياس
كم/ميل غير المفعَّلة في شاشات العرض، ودقة تسمية "أعلى نبض قلب").

---

### جلسة 2026-07-11 (الثانية) - اختبار Phase 2.5 على جهاز أندرويد حقيقي

**الوقت:** 2026-07-11، جلسة منفصلة مخصصة لاختبار جلسة "Phase 2.5: شاشة
الملف الشخصي" أدناه على جهاز حقيقي، بنفس المنهجية المستخدمة سابقاً
لاختبار Phase 1.5 (adb + uiautomator dump لاستخراج إحداثيات دقيقة +
لقطات شاشة + فحص logcat مفلتر بمعرّف عملية التطبيق).

**الجهاز المستخدم:** نفس RMX3941 (Realme, أندرويد 16) عبر USB/adb.

**نتائج الاختبار:**

1. **البناء والإطلاق:** flutter build apk --debug نجح (نفس تحذيرات NDK
   غير الضارة من قبل)، التثبيت والإطلاق نجحا بدون crash. أيقونة
   الملف الشخصي (شخص) ظهرت بشكل صحيح رابعة في AppBar بجانب الأيقونات
   الثلاث الموجودة.

2. **شاشة الملف الشخصي:** فتحت بدون أي مشكلة، عرضت القيم الافتراضية
   الصحيحة (وزن 70، عمر 30، وحدة كم، اسم فارغ) قبل أي حفظ، ومؤشر BLE
   "غير متصل" الصحيح. قسم الإحصائيات العامة عرض بيانات حقيقية من
   ride_repository (رحلة واحدة متبقية من اختبار Phase 1.5 السابق: 1
   رحلة، 0.2 كم، "--" لعدم وجود نبض قلب لتلك الرحلة) - يؤكد أن قراءة
   الإحصائيات من المستودع الفعلي تعمل، وليست بيانات وهمية.

3. **صعوبة اختبار حقيقية (وليست في التطبيق):** لوحة المفاتيح
   الافتراضية للجهاز (عربية مع اقتراحات نصية) تداخلت بشكل كبير مع
   محاولات إدخال النص عبر `adb shell input text`/`keyevent` -
   الكيبورد كان أحياناً يتجاهل نص الإدخال المُركَّب (composing text)
   عند إغلاقه بمفتاح ESCAPE بدل الانتقال لحقل آخر، ويضع المؤشر أحياناً
   في بداية النص بدل نهايته رغم KEYCODE_MOVE_END. هذه قيود أداة
   الاختبار (adb + كيبورد OEM مخصص) وليست عيوباً في حقول Flutter
   TextField نفسها - تم التحقق من أن الحقول تقبل وتعرض النص بشكل
   صحيح في كل محاولة، فقط المحتوى الدقيق المُدخَل كان صعب التحكم به
   بدقة. تم التعامل معها بإعادة المحاولة والتحقق بلقطة شاشة بعد كل
   خطوة بدل افتراض نجاح الإدخال.

4. **الحفظ:** بعد ضبط قيم صحيحة (اسم "أسعد القريشي"، وزن 75، عمر 39،
   وحدة "ميل")، ضغط زر "حفظ" أظهر Snackbar "تم حفظ الملف الشخصي" فوراً
   بدون أي خطأ في logcat الخاص بعملية التطبيق.

5. **الثبات عبر إعادة التشغيل الكاملة (الأهم):** تم إيقاف التطبيق
   بالكامل (force-stop، عملية جديدة تماماً - تم التحقق من تغيّر PID)
   ثم إعادة فتحه من الصفر والانتقال لشاشة الملف الشخصي. **كل القيم
   المحفوظة ظهرت بشكل صحيح ومطابق تماماً** (الاسم، الوزن 75، العمر 39،
   الوحدة ميل) - يؤكد أن `UserProfileRepository` يكتب فعلياً على القرص
   عبر sqflite وليس فقط في الذاكرة، وأن `main()` يقرأها بنجاح عند
   الإقلاع البارد.

6. **الاختبار الأهم - ربط الوزن الفعلي بحساب السعرات:** بعد حفظ وزن
   75كغم (بدل الافتراضي 70)، تم بدء رحلة فعلية (GPS حقيقي، بدون حزام
   نبض قلب متصل) لمدة 79 ثانية بدون حركة (0.00 كم)، ثم إنهاؤها وحفظها.
   شاشة تفاصيل الرحلة أظهرت **"7" سعرة حرارية**. التحقق اليدوي: بما أن
   لا نبض قلب متصل، يُستخدم مسار MET البديل مع avgSpeedKmh=0 → MET=4.0،
   والحساب: 4.0 × 75كغم × (79/3600) ساعة ≈ 6.58 ← تُقرَّب إلى 7. **لو
   كان الحساب لا يزال يستخدم الافتراضي القديم 70كغم بدل الوزن المحفوظ
   75، لكانت النتيجة 4.0×70×(79/3600)≈6.14 ← تُقرَّب إلى 6 وليس 7.**
   ظهور "7" بالضبط يؤكد رياضياً وبشكل قاطع أن `resolveWeightKg` سحب
   القيمة الفعلية المحفوظة من `UserProfileRepository.current` واستُخدمت
   في `estimateCaloriesBurned` عبر `ride_tracker.dart`، وليس القيمة
   الافتراضية القديمة. هذا يثبت الربط الكامل (شاشة → مستودع → sqflite
   → RideTracker → حساب السعرات → عرض في التفاصيل) يعمل فعلياً على
   جهاز حقيقي، وليس فقط في اختبارات الوحدة.

**لا أخطاء أو استثناءات أو crashes** ظهرت في logcat الخاص بعملية
التطبيق (تم الفلترة بـ PID) طوال الجلسة، عبر كل السيناريوهات أعلاه.

**Git:** لم يُنفَّذ أي commit أو push.

**الخطوة التالية:** كل ما ورد في توصية الجلسة السابقة (Phase 3 مقابل
تحسينات الخرائط) ما زال قائماً دون تغيير. بالإضافة إلى ذلك، تم الآن
التحقق الفعلي من شاشة الملف الشخصي على جهاز حقيقي، فلم يعد "اختبار
شاشة الملف الشخصي على جهاز حقيقي" بنداً معلّقاً - يمكن الانتقال مباشرة
لـ Phase 3 أو لمعالجة الديون التقنية الموثّقة (وحدة القياس غير
المفعَّلة، ودقة "أعلى نبض قلب") دون الحاجة لجلسة اختبار جهاز إضافية
لهذا الجزء تحديداً.

---

### جلسة 2026-07-11 - Phase 2.5: شاشة الملف الشخصي

**الوقت:** 2026-07-11، جلسة واحدة، تبني فوق Phase 1/1.5/2 القائمة دون
تعديل منطق GPS أو منطق اتصال/اقتران BLE (كما طُلب صراحة).

**ملاحظة عن مصدر المهمة:** طُلبت هذه الجلسة عبر ملف تعليمات خارجي
(`/tmp/task.txt`) بدل الكتابة المباشرة في المحادثة. قبل تنفيذ أي شيء،
تم التحقق من كل الافتراضات التي بناها الملف على الكود الفعلي
(وجود `calorie_calculator.dart` بالضبط بالـ placeholders الموصوفة،
نمط تخزين `ride_repository.dart` الفعلي، تصميم التنقل الفعلي في
`tracking_screen.dart`) قبل البدء - كل الافتراضات كانت صحيحة فعلياً،
فتمت المتابعة. نقطتان اختلفتا عن تخمين الملف وتم اتّباع الواقع الفعلي
بدلاً من التخمين:
1. التخزين: الملف افترض "على الأرجح Hive أو shared_preferences" -
   لكن لا وجود لأي منهما في المشروع؛ التخزين الفعلي الوحيد هو sqflite
   (المستخدم في `ride_repository.dart`)، فاستُخدم sqflite لجدول
   الملف الشخصي أيضاً بدل إضافة تبعية جديدة (يطابق تعليمات الملف نفسه
   بعدم إضافة تبعية جديدة).
2. التنقل: الملف افترض "على الأرجح Drawer أو bottom nav" - لكن
   النمط الفعلي المستخدم هو أيقونات AppBar actions (كما في أيقونات
   الخريطة/نبض القلب/السجل)، فأُضيفت أيقونة شخص بنفس النمط بدل اختراع
   نمط تنقل جديد.

**الملفات الجديدة:**
- `lib/models/user_profile.dart` - `UserProfile` بالحقول المطلوبة
  بالضبط (name, weightKg افتراضي 70, age افتراضي 30, preferredUnit
  enum km/miles افتراضي km) - القيم الافتراضية مطابقة عمداً لثوابت
  `placeholderWeightKg`/`placeholderAgeYears` الموجودة مسبقاً في
  calorie_calculator.dart، بحيث لا يتغير أي سلوك قبل حفظ أي ملف فعلياً.
- `lib/services/app_database.dart` - استُخرج منطق فتح قاعدة sqflite
  المشترك (كان مضمّناً سابقاً داخل `ride_repository.dart` فقط) إلى
  ملف واحد يستخدمه كل من `RideRepository` و`UserProfileRepository`،
  بدل أن يملك كل منهما تعريف onCreate/onUpgrade مستقلاً لنفس ملف
  القاعدة (وهذا كان سيؤدي لتضارب/انحراف بين التعريفين مستقبلاً). رُفعت
  `_dbVersion` من 1 إلى 2 مع `onUpgrade` يضيف جدول `user_profile`
  الجديد للتركيبات القديمة دون المساس بجدولي `rides`/`ride_points`
  الحاليين. يقبل مساراً اختيارياً (`overridePath`) للاستخدام في
  الاختبارات فقط.
- `lib/services/user_profile_repository.dart` - `load()`/`save()` عبر
  sqflite (صف واحد فقط، مفروض عبر `CHECK (id = 1)` +
  `ConflictAlgorithm.replace`)، مع كاش متزامن (`current`) يتيح لـ
  `RideTracker` قراءة القيم دون الحاجة لجعل `finishRide()` غير متزامنة.
- `lib/screens/profile_screen.dart` - حقول تحرير (اسم/وزن/عمر/وحدة
  القياس عبر SegmentedButton)، قسم إحصائيات للقراءة فقط (إجمالي
  المسافة، عدد الرحلات، أعلى متوسط نبض قلب لرحلة - انظر ملاحظة القرار
  أدناه) محسوبة مباشرة عبر استعلام `RideRepository.getAllRidesSummary()`
  (بدون نسخ بيانات الرحلات لمخزن منفصل)، ومؤشر حالة اتصال BLE للقراءة
  فقط (بدون أي منطق اقتران جديد).
- `test/user_profile_repository_test.dart` - 5 اختبارات (قيم افتراضية
  قبل أي حفظ، قيمة `current` قبل استدعاء `load()`، حفظ ثم تحميل من
  كائن جديد تماماً للتأكد من كتابة/قراءة حقيقية على القرص، تحديث
  `current` فوراً بعد `save()` دون إعادة تحميل، الحفظ مرتين يُحدّث نفس
  الصف بدل إضافة صف ثانٍ).
- **تبعية اختبار جديدة (dev فقط):** `sqflite_common_ffi` (تحلّل إلى
  2.3.6) - لازمة لأن sqflite تعتمد على platform channel حقيقي (لا يوجد
  في بيئة `flutter test` VM)؛ هذه الحزمة تُشغّل sqlite3 حقيقية عبر FFI
  بدلاً من ذلك، وهي الطريقة القياسية والموثقة لاختبار كود سqflite. لا
  علاقة لها بتبعيات الإنتاج (production) ولا تُغيّر طريقة التخزين
  الفعلية في التطبيق - فقط تمكّن اختبارها.

**الملفات المعدّلة:**
- `lib/utils/calorie_calculator.dart` - إضافة دالتين نقيّتين صغيرتين:
  `resolveWeightKg(double?)` و`resolveAgeYears(int?)` - تُرجعان القيمة
  المُمرَّرة إن وُجدت، أو الـ placeholder الموثّق إن كانت null. هذا هو
  منطق "السقوط للقيم الافتراضية" المطلوب، مطبَّق كدوال نقية قابلة
  للاختبار المباشر بدل منطق مبعثر داخل `ride_tracker.dart`.
- `lib/services/ride_tracker.dart` - يقبل الآن `UserProfileRepository?`
  اختيارياً (بنفس نمط `HeartRateService?` الموجود)؛ عند `finishRide()`
  يمرر `resolveWeightKg(profile?.weightKg)` و`resolveAgeYears(profile?.age)`
  بدل الاعتماد على القيم الافتراضية لصيغة `estimateCaloriesBurned` مباشرة.
- `lib/services/ride_repository.dart` - أصبحت تستخدم `openAppDatabase()`
  المشتركة من `app_database.dart` بدل تعريف schema مستقل بداخلها؛ لا
  تغيير في أي من دوالها العامة (`saveRide`, `getAllRidesSummary`,
  `getRideWithPoints`, `deleteRide`) ولا في جدولي `rides`/`ride_points`
  أنفسهما.
- `lib/main.dart` - إنشاء `UserProfileRepository` واحدة، استدعاء
  `load()` عليها قبل `runApp` (بنفس نمط `mapTileService.init()`
  الموجود)، تمريرها لـ `RideTracker` وإضافتها لـ `MultiProvider`.
  `MyApp` يتطلب الآن `userProfileRepository` كمعامل إضافي.
- `lib/screens/tracking_screen.dart` - إضافة أيقونة شخص (`Icons.person_outline`)
  في AppBar تفتح `ProfileScreen`، بنفس نمط الأيقونات الثلاث الموجودة.
- `test/widget_test.dart` - تمرير `UserProfileRepository()` طازجة (بدون
  `load()`) لـ `MyApp`، لأن الاختبار لا يستدعي أي مسار يحتاجها فعلياً
  فتبقى بقيمها الافتراضية في الذاكرة دون لمس أي قناة platform حقيقية.
- `test/calorie_calculator_test.dart` - 4 اختبارات جديدة لـ
  `resolveWeightKg`/`resolveAgeYears` (تمرير القيمة، والسقوط للـ
  placeholder عند null).

**قرار مهم يستحق المراجعة - "أعلى نبض قلب مسجَّل":** طلبت التعليمات
عرض "highest recorded BPM" في قسم الإحصائيات. الفحص الفعلي كشف أن
التطبيق **لا يخزّن** أعلى نبض قلب لحظي (peak instantaneous BPM) لأي
رحلة إطلاقاً - العمود الوحيد المحفوظ هو `avg_heart_rate` (متوسط الرحلة
كاملة، من Phase 2)، وليس فيه أي تتبع لأعلى قراءة لحظية أثناء الرحلة.
بدل اختراع عمود جديد (`max_heart_rate` لكل رحلة) وتعديل `ride_tracker.dart`
لتتبعه (توسّع في النطاق يتجاوز ما طُلب، ويحتاج migration جديد)، تم
اختيار: عرض "أعلى متوسط نبض لرحلة" (max من `avg_heart_rate` عبر كل
الرحلات) بتسمية صادقة في الواجهة بدل الإدعاء بدقة غير موجودة فعلياً في
البيانات. **هذا قرار يستحق المراجعة من المطوّر** - إن كانت "أعلى نبضة
لحظية فعلية" مطلوبة فعلاً، فهذا يحتاج جلسة منفصلة تضيف تتبع peak BPM
لكل رحلة في `ride_tracker.dart` + عمود جديد + migration، وليس مجرد
عرض.

**قرار آخر غير مكتمل عمداً - وحدة القياس (كم/ميل):** تخزين/تحرير
`preferredUnit` في شاشة الملف الشخصي **تم بناؤه بالكامل ويُحفظ فعلياً**،
لكنه **غير مُفعَّل في أي مكان آخر بالتطبيق** - كل عروض المسافة في
`tracking_screen.dart`/`ride_history_screen.dart`/`ride_detail_screen.dart`
ما زالت بالكيلومتر ثابتاً بغض النظر عن قيمة `preferredUnit` المحفوظة.
لم تطلب التعليمات ربط التحويل الفعلي في كل هذه الشاشات، وتوسيع النطاق
لتغطيتها كان سيلامس شاشات لا علاقة لها بهذه الجلسة. **هذا TODO صريح
لمرحلة قادمة (Phase 3 أو جلسة منفصلة).**

**نتيجة flutter analyze:** نجح - "No issues found!"

**نتيجة flutter test:** نجحت 31/31 اختباراً (22 اختباراً موروثاً من
المراحل السابقة دون أي تغيير في نتيجتها + 5 اختبارات جديدة في
`user_profile_repository_test.dart` + 4 اختبارات جديدة في
`calorie_calculator_test.dart`).

**لم يُختبر على جهاز حقيقي في هذه الجلسة** (بخلاف جلسة الاختبار
اليدوي السابقة لـ Phase 1.5) - التحقق اقتصر على flutter analyze/test
ومراجعة الكود. يوصى باختبار شاشة الملف الشخصي فعلياً (خاصة حفظ/تحميل
القيم عبر إعادة تشغيل التطبيق) على جهاز حقيقي قبل الانتقال لأي مرحلة
تالية.

**Git:** لم يُنفَّذ أي commit أو push، حسب التعليمات الصريحة (سواء من
الملف أو القاعدة الثابتة في هذا الملف).

**الخطوة التالية:** يوصى بأحد اثنين: (أ) اختبار شاشة الملف الشخصي على
جهاز حقيقي (حفظ ملف، إغلاق التطبيق، إعادة فتحه، التأكد من ظهور القيم
المحفوظة، وإنهاء رحلة فعلية والتأكد أن السعرات المحسوبة تعكس الوزن/العمر
الجديدين فعلاً)، أو (ب) البدء بـ Phase 3 (تحذيرات صوتية) مع ترك TODOين
أعلاه (وحدة القياس، ودقة "أعلى نبض") كديون تقنية موثّقة يعود لهما
المطوّر لاحقاً.

---

### جلسة 2026-07-10 (الثالثة) - Phase 2: تكامل BLE لنبض القلب + حساب السعرات

**الوقت:** 2026-07-10، من حوالي الساعة 15:43 حتى 16:02 (حسب ساعة الجهاز
الحقيقي المستخدم في الاختبار)، جلسة واحدة متواصلة تبني على أساس Phase 1
وPhase 1.5 القائمين (لم يُعَد بناء أي منهما من الصفر، ولم يُعدَّل منطق
GPS tracking الأساسي إطلاقاً).

**ملخص ما تم إنجازه:** إضافة اتصال BLE بحزام/ساعة نبض قلب معيار
Bluetooth SIG القياسي (خدمة 0x180D)، بالإضافة إلى حساب سعرات حرارية
تقديري، فوق نواة GPS + الخرائط offline القائمة من المرحلتين السابقتين.

**الملفات الجديدة:**
- `lib/utils/heart_rate_parser.dart` - دالة `parseHeartRateBpm` تفسّر
  بايتات خاصية Heart Rate Measurement (0x2A37) حسب معيار Bluetooth GATT
  الرسمي، تتعامل بشكل صحيح مع كلا ترميزي UINT8 وUINT16 (bit0 من بايت
  flags)، وتُرجع null بدل رمي استثناء عند بيانات غير مكتملة.
- `lib/services/heart_rate_service.dart` - يغلّف flutter_reactive_ble:
  فحص (scan) عن خدمة 0x180D فقط، دفق (stream) للأجهزة المكتشفة، اتصال
  بجهاز محدد، اشتراك بخاصية القياس وتحويلها لدفق `Stream<int>` من BPM،
  إعادة محاولة تلقائية واحدة عند انقطاع الاتصال ثم استقرار على حالة
  "غير متصل" بدل التعليق أو الانهيار. يحتوي أيضاً على واجهة `BleClient`
  مستخرجة (تُنفَّذ افتراضياً بـ `ReactiveBleClient` فوق
  `FlutterReactiveBle` الحقيقية) لتمكين حقن كائن وهمي (fake) في
  الاختبارات، لأن `FlutterReactiveBle()` الحقيقية تبدأ تهيئة async فوق
  قناة platform channel فور إنشائها، مما يسبب استثناء `UnimplementedError`
  في بيئة اختبار VM بدون قناة BLE حقيقية.
- `lib/screens/heart_rate_pairing_screen.dart` - شاشة عربية تعرض
  الأجهزة المكتشفة (اسم، RSSI) مع زر "اتصال" لكل جهاز، وحالة اتصال
  واضحة (بحث/اتصال جارٍ/متصل/غير متصل) في الأعلى.
- `lib/utils/calorie_calculator.dart` - دالة `estimateCaloriesBurned`:
  صيغة Keytel et al. (2005) المعتمدة على نبض القلب عند توفره (صيغتان
  منفصلتان للذكور والإناث عبر معامل `Gender`)، مع سقوط (fallback) لصيغة
  MET قياسية معتمدة على متوسط السرعة عند عدم توفر نبض القلب (جدول
  Compendium of Physical Activities لركوب الدراجات). ثوابت الوزن
  (70كغم) والعمر (30 عاماً) موثّقة صراحة كـ placeholder مؤقت مع تعليق
  TODO يوضح الحاجة لشاشة ملف مستخدم حقيقية لاحقاً.
- `test/heart_rate_parser_test.dart` - 6 اختبارات: ترميز UINT8، ترميز
  UINT16 (بما فيها حالة قيمة صغيرة <256 لضمان عدم اقتطاعها كـ UINT8
  خطأً)، ومصفوفة بايتات فارغة/ناقصة لكل من الترميزين بدون رمي استثناء.
- `test/calorie_calculator_test.dart` - 6 اختبارات: قيمة موجبة معقولة
  لصيغة نبض القلب (ذكر وأنثى)، قيمة موجبة معقولة لصيغة MET البديلة (مع
  وبدون سرعة معروفة)، وعدم انهيار division-by-zero لرحلة بمدة صفر
  (لكلا المسارين).

**الملفات المعدّلة:**
- `pubspec.yaml` - إضافة `flutter_reactive_ble: ^5.4.0` (تحلّل إلى
  5.4.2) و`permission_handler: ^11.3.1` (تحلّل إلى 11.4.0، أُضيفت لاحقاً
  أثناء الجلسة بعد اكتشاف مشكلة حقيقية، انظر أدناه). لا تعارضات تبعية
  (dependency conflicts).
- `android/app/src/main/AndroidManifest.xml` - إضافة
  `BLUETOOTH_SCAN` (مع `android:usesPermissionFlags="neverForLocation"`
  لأن التطبيق لا يستخدم فحص BLE لتحديد الموقع الجغرافي، لديه GPS منفصل
  لذلك) و`BLUETOOTH_CONNECT`، بالإضافة إلى `BLUETOOTH`/`BLUETOOTH_ADMIN`
  القديمتين بـ `maxSdkVersion="30"` للتوافق مع ما قبل أندرويد 12.
  `minSdkVersion` بقي 21 (القيمة الافتراضية من Phase 1) وهو متوافق.
- `lib/services/ride_tracker.dart` - يحمل الآن `HeartRateService?`
  اختياري بالكامل (لا يُجبر أي رحلة على وجود مستشعر نبض). أثناء التتبع
  النشط، يتجمّع متوسط BPM الوارد من `bpmStream`، وعند `finishRide()`
  يُحسب `avgHeartRate` (null إن لم يتصل أي مستشعر) ثم يُستدعى
  `estimateCaloriesBurned` لملء `caloriesBurned` - كلا الحقلين كانا
  موجودين مسبقاً في نموذج `Ride` منذ Phase 1 (nullable، مُعدّان لهذا
  الغرض بالضبط).
- `lib/main.dart` - إنشاء `HeartRateService` واحدة (singleton) في
  `main()`، إضافتها لـ `MultiProvider` القائم، وتمريرها لبناء
  `RideTracker`. `MyApp` أصبح يتطلب `heartRateService` كمعامل إضافي.
- `lib/screens/tracking_screen.dart` - إضافة أيقونة قلب في AppBar
  (بجانب أيقونتَي الخريطة وسجل الرحلات من Phase 1.5) تفتح شاشة
  الاقتران، تتلوّن أحمر عند الاتصال. إضافة صف إحصائية جديد "نبض القلب"
  (بإعادة استخدام `StatColumn` الموجودة) بجانب "ارتفاع التسلق"، يعرض
  BPM الحي عند الاتصال أو "غير متصل" (وليس صفراً مربكاً) عند عدم
  الاتصال.
- `lib/screens/ride_detail_screen.dart` - عرض `avgHeartRate` و
  `caloriesBurned` (بإعادة استخدام `StatColumn`) فقط إن كانا موجودين
  (`if (ride.avgHeartRate != null) ...`)، فلا يظهر شيء لرحلات Phase 1
  القديمة أو الرحلات بدون مستشعر.
- `test/widget_test.dart` - تمرير `HeartRateService` مع `_NoOpBleClient`
  (تنفيذ وهمي لواجهة `BleClient` الجديدة، دفوق فارغة دائماً) بدل إنشاء
  `HeartRateService()` حقيقية، لتفادي استثناء التهيئة async المذكور
  أعلاه أثناء اختبار الواجهة.

**نسخة flutter_reactive_ble المستخدمة:** 5.4.2 (النسخة 5.5.0 موجودة
على pub.dev لكن غير قابلة للحل ضمن قيود التبعيات الحالية حسب
`flutter pub outdated`؛ لا مشكلة، 5.4.2 تحقق المطلوب: "أحدث نسخة مستقرة
متوافقة"). `permission_handler` المستخدمة: 11.4.0.

**نتيجة flutter analyze:** نجح - "No issues found!" (تم التحقق 4 مرات
منفصلة عبر الجلسة بعد كل تعديل جوهري).

**نتيجة flutter test:** نجحت 22/22 اختباراً (16 اختباراً موروثاً من
Phase 1/1.5 دون أي تغيير في نتيجتها + 6 اختبارات جديدة في
`heart_rate_parser_test.dart` + 6 اختبارات جديدة في
`calorie_calculator_test.dart`؛ ملاحظة: 16 = 5 distance_calculator + 1
widget_test + 4 map_tile_service + 6 كانت من قبل... بالتفصيل: 5 + 1 + 4
+ 6[parser] + 6[calorie] = 22).

**اختبار BLE على جهاز حقيقي - تم فعلياً، نتائج حقيقية وليست محاكاة:**

الجهاز: نفس RMX3941 (Realme) من جلسات Phase 1/1.5 السابقة، متصل عبر
adb. تم بناء `flutter build apk --debug` وتثبيته فعلياً عبر
`adb install -r` (أُعيد 3 مرات مع كل إصلاح جوهري). التفاعل عبر
`adb shell input tap` بعد استخراج إحداثيات دقيقة بـ `uiautomator dump`
في كل مرة (نفس درس RTL من الجلسة السابقة)، مع لقطات شاشة (`screencap`)
للتحقق البصري و`logcat` مفلترة بـ PID للتحقق من عدم وجود استثناءات.
الفيتبت المستخدم: Fitbit Charge 6 حقيقي للمطوّر، بوضع "HR on equipment"
مفعّل يدوياً من قبل المطوّر (لا يمكن للمساعد تفعيله فعلياً - يتطلب لمس
الساعة الفعلي).

**ما تم التحقق منه فعلياً على الجهاز الحقيقي:**
1. التطبيق يُثبَّت ويُطلَق بدون أي crash، ولا رجعية (regression) في
   وظائف Phase 1/1.5 (ظهرت الخريطة، الإحصائيات، صف "نبض القلب" الجديد
   بحالة "غير متصل" بشكل صحيح من أول إطلاق).
2. **اكتُشفت وأُصلحت مشكلة حقيقية رقم 1:** إذنا `BLUETOOTH_SCAN` و
   `BLUETOOTH_CONNECT` كانا مُعرَّفين في AndroidManifest لكن لم يُطلَبا
   فعلياً وقت التشغيل (`flutter_reactive_ble` لا يطلب أذونات وقت التشغيل
   بنفسه على أندرويد 12+). تم التحقق عبر
   `adb shell dumpsys package ... | grep BLUETOOTH_SCAN` أن الإذن كان
   `granted=false` رغم تعريفه في المانيفست، والفحص (scan) كان يفشل
   بصمت بدون أي مربع حوار نظام يظهر للمستخدم. تم إصلاحه بإضافة حزمة
   `permission_handler` واستدعاء `[Permission.bluetoothScan,
   Permission.bluetoothConnect].request()` قبل بدء الفحص في
   `HeartRateService.startScan()`. بعد الإصلاح: ظهر مربع حوار النظام
   الحقيقي بالعربية وتم قبوله، وتحوّل الإذن إلى `granted=true` فعلياً
   (تم التحقق).
3. الفحص عن أجهزة خدمة 0x180D نجح بشكل موثوق ومتكرر: اكتُشف جهاز
   "Charge 6" الحقيقي بقيم RSSI حقيقية متفاوتة (-33 إلى -39) عبر عدة
   محاولات فحص منفصلة.
4. **اكتُشفت وأُصلحت مشكلة حقيقية رقم 2 (واجهة):** عند إعادة فتح شاشة
   الاقتران بعد اتصال ناجح سابقاً، ظهرت "لم يتم العثور على أي جهاز بعد"
   رغم أن الحالة العلوية أظهرت "متصل" فعلياً - السبب: `discoveredDevices`
   دفق broadcast لا يُعيد بث الأحداث الماضية لمستمع (listener) جديد. تم
   إصلاحه بإضافة `discoveredDevicesSnapshot` (قائمة فورية من آخر حالة
   معروفة) واستخدامه كـ `initialData` لـ `StreamBuilder` في شاشة
   الاقتران.
5. **اكتُشفت وأُصلحت مشكلة حقيقية رقم 3 (الأهم):** محاولات الاتصال
   الأولى بقيت عالقة على "جارٍ الاتصال..." لأكثر من 30 ثانية بدون
   استقرار، مع دورات متكررة من `BluetoothGatt: connect() ... auto: true`
   في logcat. السبب: عدم تمرير `connectionTimeout` لـ
   `FlutterReactiveBle.connectToDevice()` يجعل المكتبة تستخدم
   `autoConnect=true` في أندرويد (وضع إعادة اتصال خلفي بطيء وغير مناسب
   لاتصال مباشر بجهاز يبثّ إعلاناته حالياً). تم إصلاحه بتمرير
   `connectionTimeout: Duration(seconds: 10)`، وتم التحقق عبر logcat أن
   الاتصالات اللاحقة أصبحت فعلاً `auto: false` (اتصال مباشر).
6. **بعد الإصلاحات الثلاثة أعلاه، سلوك التطبيق أصبح صحيحاً ومتوقعاً
   بالكامل، لكن الاتصال الفعلي (GATT) بجهاز Charge 6 استمر بالفشل** -
   `logcat` أظهر `BluetoothGatt: onClientConnectionState() status=147
   ... connected=false` بشكل متكرر عبر عدة محاولات (147 = رمز خطأ
   GATT_CONN_TIMEOUT في مكدّس Bluetooth الخاص بأندرويد). هذا **فشل على
   مستوى نظام التشغيل/مكدّس Bluetooth الفعلي للجهاز، وليس خطأً في كود
   Dart** - مشكلة معروفة على بعض أجهزة Android المبنية على ColorOS
   (Realme/Oppo) مع بعض الأجهزة الطرفية BLE.
7. **السلوك الأهم للتحقق منه نجح رغم فشل الاتصال:** بعد فشل محاولة
   إعادة الاتصال التلقائية الواحدة، استقرّ التطبيق فعلياً على حالة
   "غير متصل" بدون أي تعليق أو تجمّد أو انهيار في الواجهة - وهذا يطابق
   بالضبط متطلب "يجب ألا يتوقف RideTracker أو ينهار عند انقطاع
   المستشعر"، **تم التحقق منه فعلياً تحت فشل حقيقي، وليس افتراضاً نظرياً
   فقط.**
8. لا استثناءات أو crashes خاصة بعملية التطبيق ظهرت في logcat (مفلترة
   بـ PID) طوال كل محاولات الاختبار.

**لم يتحقق:** لم يُستلَم أي قيمة BPM فعلية من الفيتبت في هذه الجلسة
(بما أن الاتصال GATT نفسه لم ينجح أبداً، فمنطق تفسير البايتات في
`heart_rate_parser.dart` لم يُختبر إلا عبر اختبارات الوحدة، وليس ببيانات
حقيقية من الجهاز). تم عرض الخيار على المطوّر (محاولات إضافية عبر تبديل
البلوتوث/تقريب الساعة، أو التوقف والتوثيق) واختار المطوّر التوقف
والتوثيق كمشكلة معروفة بدل الاستمرار بالتخمين على مستوى مكدّس نظام
التشغيل.

**قيم Placeholder المستخدمة في حساب السعرات:** الوزن = 70 كغم، العمر
= 30 عاماً، الجنس الافتراضي = ذكر (`Gender.male`، قابل للتبديل عبر
معامل). موثّقة صراحة بتعليق `TODO(user-profile)` في
`calorie_calculator.dart`. **الحاجة لشاشة ملف مستخدم حقيقية (وزن، عمر،
جنس) تبقى متطلباً مستقبلياً غير مبني بعد.**

**مشاكل غير محلولة:**
- **الأهم:** فشل اتصال GATT الفعلي بـ Fitbit Charge 6 على جهاز
  Realme RMX3941 تحديداً (`status=147` / GATT_CONN_TIMEOUT) - **تم
  إجراء محاولتَين إضافيتين بعد كتابة هذا القسم أول مرة، وكلاهما فشلتا
  بنفس رمز الخطأ بالضبط:**
  1. إيقاف/تشغيل البلوتوث بالكامل من النظام
     (`adb shell svc bluetooth disable` ثم `enable`، تم التحقق من
     `enabled: true` فعلياً عبر `dumpsys`) ثم إعادة محاولة الاتصال:
     **فشلت بنفس `status=147`.**
  2. تقريب الساعة جسدياً (RSSI تحسّن إلى -33، إشارة قوية) مع إعادة
     تفعيل "HR on equipment" من الصفر على الساعة، ثم محاولتا اتصال
     (الأولى + إعادة المحاولة التلقائية الواحدة): **كلتاهما فشلتا بنفس
     `status=147` بالضبط.**

  بما أن الفشل ثابت ومحدَّد (نفس رمز الخطأ) بغض النظر عن قوة الإشارة أو
  إعادة تشغيل البلوتوث أو إعادة تفعيل بث الساعة، فهذا يستبعد أسباباً
  مؤقتة (تداخل، بعد المسافة، بث قديم عالق) ويُرجّح بقوة أن السبب هو
  عدم توافق ثابت بين مكدّس Bluetooth الخاص بـ ColorOS/Realme على هذا
  الجهاز تحديداً وخادم GATT الخاص بالفيتبت - **لم يُختبر بعد على جهاز
  أندرويد آخر غير قائم على ColorOS، وهذا هو الاختبار التالي الأكثر
  فائدة لعزل المتغيّر فعلياً** (إن توفّر جهاز آخر - Pixel/Samsung/إلخ).
- منطق تفسير بايتات BPM (`parseHeartRateBpm`) مُختبر فقط عبر اختبارات
  الوحدة المعزولة (unit tests) ببيانات مُصطنعة تطابق معيار Bluetooth
  الرسمي بدقة - لم يُتحقق منه بعد ببيانات فعلية واردة من جهاز Fitbit
  حقيقي، بسبب فشل الاتصال أعلاه.
- شاشة ملف مستخدم/إعدادات لتغذية حاسبة السعرات بوزن/عمر/جنس حقيقيين لا
  تزال غير مبنية (متروكة عمداً حسب تعليمات نطاق هذه الجلسة).

**الخطوة التالية (لأي جلسة قادمة، بدون أي سياق مسبق):** القرار متروك
للمطوّر بين ثلاثة مسارات:

1. **إكمال متانة BLE (الأولوية المقترحة، امتداد مباشر لعمل هذه
   الجلسة):** حل مشكلة `GATT_CONN_TIMEOUT` (status=147) الموثّقة أعلاه،
   والتحقق الفعلي من استلام قيم BPM حقيقية من الفيتبت لأول مرة - كل
   الكود المطلوب لهذا موجود ومختبَر بالوحدة (unit-tested)، الناقص فقط
   هو نجاح الاتصال الفعلي على مستوى نظام التشغيل.
2. **شاشة ملف المستخدم/الإعدادات:** بناء شاشة إدخال وزن/عمر/جنس حقيقية
   لتغذية `estimateCaloriesBurned` بقيم حقيقية بدل الـ placeholder
   (70كغم/30 عاماً/ذكر) الموثّق أعلاه.
3. **Phase 3 (تحذيرات مخاطر مرورية صوتية):** الانتقال للمرحلة التالية
   في الخطة الأصلية، إن قرر المطوّر تأجيل حل مشكلة GATT.

كل المسارات الثلاثة لم يُبدأ فيها أي عمل بعد.

**ملاحظة git:** لم يتم تنفيذ أي git commit أو push في هذه الجلسة، حسب
تعليمات المهمة الصريحة. كل التغييرات أعلاه غير مضمَّنة (uncommitted)
بانتظار المراجعة اليدوية من المطوّر. (ملاحظة: المشروع في
`~/projects/iraq_cycling` لا يحتوي حالياً على مستودع git مهيّأ - أمر
`git status` أعاد "fatal: not a git repository" - قد يكون تم حذف
`.git` بعد الجلسة التي ذكرت تفعيله، أو كان ذلك في مسار مختلف؛ هذا غير
متعلق بعمل هذه الجلسة ولم يُعالَج لأنه خارج النطاق ولم يُطلَب.)

**تأكيد:** تم كتابة هذا القسم فعلياً إلى PROJECT_STATE.md على القرص
(وليس فقط وصفه في ملخص المحادثة).

---

### جلسة 2026-07-10 (الثانية) - اختبار Phase 1.5 على جهاز أندرويد حقيقي

**الوقت:** بعد ظهر نفس يوم جلسة إكمال Phase 1.5، جلسة منفصلة مخصصة
فقط للاختبار الفعلي على جهاز حقيقي (لم يكن متاحاً في الجلسة السابقة).

**الجهاز المستخدم:** RMX3941 (Realme)، أندرويد 16 (API 36)، متصل عبر
USB/adb، متصل بشبكة WiFi حقيقية ("Ahssan_5G") بإنترنت فعلي.

**طريقة الاختبار:** flutter build apk --debug ثم adb install، بعدها
تفاعل فعلي مع التطبيق عبر adb shell input tap (بعد استخراج الإحداثيات
الدقيقة للأزرار عبر uiautomator dump في كل مرة، لأن التخطيط RTL
العربي يعكس مواضع الأزرار بشكل غير بديهي) + لقطات شاشة (screencap)
للتحقق البصري + logcat للتحقق من عدم وجود أخطاء أو exceptions خاصة
بالتطبيق (تم الفلترة بمعرّف عملية التطبيق (PID) لتفادي ضجيج النظام).
هذه ليست ركوب دراجة فعلي (الجهاز كان ثابتاً طوال الاختبار)، لكنها
تحقق فعلي من التشغيل على جهاز حقيقي وليس محاكاة.

**نتائج الاختبار (كل ما يلي تم التحقق منه فعلياً، وليس افتراضاً):**

1. **الإطلاق الأول:** لا crash. الخريطة الحية تظهر بلاطات OSM حقيقية
   مركزّة على بغداد الافتراضية (حي المنصور، متنزه الزوراء) فوراً بعد
   الإطلاق - يؤكد أن التخزين المؤقت offline-first يعمل ويجلب من
   الشبكة بنجاح. الشريط العلوي (أيقونتا سجل الرحلات وتحميل الخريطة)
   والإحصائيات وزر "ابدأ الرحلة" كلها تُعرض بشكل مطابق لتصميم Phase 1.

2. **إذن الموقع:** ظهر مربع حوار نظام أندرويد الحقيقي لطلب إذن
   الموقع (بالعربية، مع خياري "دقيق"/"تقريبي" وثلاثة أزرار قبول/رفض).
   تم منح "أثناء استخدام التطبيق" بنجاح، وبدأ GPS الحقيقي بالعمل
   فوراً - الجهاز التقط موقعه الفعلي (منطقة الكاظمية قرب النهر، وليس
   مركز بغداد الافتراضي)، مؤكداً أن GPS الحقيقي وليس بيانات وهمية.

3. **حالات الأزرار الأربع (Phase 1 الأساسية):** تم اختبار البدء
   والإيقاف المؤقت والاستئناف والإنهاء بالكامل على الجهاز الحقيقي.
   كل حالة أظهرت الأزرار الصحيحة (ابدأ/إيقاف مؤقت+إنهاء/استئناف+إنهاء/
   رحلة جديدة) والإحصائيات تحدثت حياً (كم/س، وقت، مسافة). عند الإنهاء
   ظهر Snackbar "تم حفظ الرحلة: 0.21 كم" مؤكداً الحفظ الفعلي في
   sqflite.

4. **سجل الرحلات وتفاصيل الرحلة:** الرحلة المحفوظة ظهرت في سجل
   الرحلات ببطاقة صحيحة (تاريخ، مسافة، وقت، معدل). فتح تفاصيل الرحلة
   أظهر polyline فعلي للمسار (خط متعرج يعكس ضجيج GPS الحقيقي أثناء
   وقوف الجهاز ثابتاً) مع CameraFit.coordinates يُقرّب الخريطة بشكل
   صحيح على حدود المسار، والإحصائيات النهائية (المدة، المسافة، أقصى
   سرعة 4.0، معدل 3.9، الارتفاع) صحيحة ومطابقة لما تم حفظه.

5. **شاشة تحميل الخريطة:** العدد التقريبي المحسوب فعلياً على الجهاز
   = 64109 بلاطة (~939 ميجابايت تقريباً) لمنطقة بغداد بمستويات
   التكبير 10-17. تم تشغيل التحميل الفعلي والتحقق من: (أ) شريط
   التقدم يتحرك فعلياً (RTL، يمتلئ من اليمين لليسار)، (ب) ملفات PNG
   حقيقية تُكتب على القرص بالفعل بمسار
   `files/iraq_cycling_tiles/{z}/{x}/{y}.png` (تم التحقق عبر
   `adb shell run-as`)، (ج) لا استثناءات أو أخطاء في logcat خاصة
   بعملية التطبيق طوال التحميل. **تم إيقاف التحميل يدوياً بعد ~538
   بلاطة (~7.8 ميجابايت) فقط** ولم يُترك ليكتمل (سيستغرق ساعات
   ويُحمّل عشرات آلاف الطلبات من خادم OSM المشترك) - هذا اختيار واعٍ
   متسق مع ملاحظة سياسة OSM الموثقة في الجلسة السابقة، وليس فشلاً في
   الاختبار. **لم يتم اختبار اكتمال التحميل الكامل فعلياً - يبقى هذا
   افتراضاً غير مُتحقق منه.**

6. **السيناريو الأهم: عدم توفر إنترنت + بلاطة غير مخزَّنة (البلاطة
   الرمادية البديلة):** تم إيقاف WiFi وبيانات الجوال فعلياً على
   الجهاز (`svc wifi disable` و`svc data disable`)، ثم إعادة تشغيل
   التطبيق من الصفر (بعد force-stop) للتأكد من عدم الاعتماد على أي
   بيانات في الذاكرة. النتيجة:
   - المنطقة المخزَّنة مسبقاً (مركز بغداد الافتراضي عند مستوى التكبير
     المبدئي) ظهرت بكامل تفاصيلها من الكاش المحلي بدون أي مشكلة رغم
     انعدام الإنترنت الفعلي.
   - بعد التكبير (zoom in عبر نقر مزدوج متكرر) إلى مستوى لم تُحمَّل
     بلاطاته مسبقاً، **ظهرت البلاطة الرمادية المحايدة فعلياً على
     الشاشة** بدل أي شرخ أو أيقونة خطأ أو انهيار - هذا يطابق تماماً
     المتطلب الأصعب في المهمة الأصلية ("لا إنترنت + لا بلاطة محفوظة
     = بلاطة رمادية، ليس crash"). تم التحقق البصري المباشر عبر لقطة
     شاشة، وليس فقط منطقياً بمراجعة الكود.
   - تمت إعادة تفعيل WiFi وبيانات الجوال على الجهاز في نهاية الاختبار
     (لم يُترك الجهاز في وضع بلا إنترنت).

**مشاكل أو ملاحظات من الاختبار الفعلي:**
- لا أخطاء ولا استثناءات ولا crashes ظهرت في logcat الخاص بعملية
  التطبيق (تم التحقق بفلترة PID) في أي من السيناريوهات أعلاه.
- سهو بسيط أثناء الاختبار (وليس في التطبيق نفسه): إحداثيات النقر
  الأولى المحسوبة يدوياً كانت خاطئة مرتين (بسبب التخطيط RTL وتغيّر
  حجم منطقة الإحصائيات القابلة للتمرير) - تم حله عبر uiautomator dump
  لاستخراج الإحداثيات الدقيقة لكل زر بدل الحساب اليدوي التقريبي. هذا
  درس لأي اختبار لاحق عبر adb على هذا التطبيق تحديداً بسبب RTL.
  ليس عيباً في كود التطبيق.
- لم يتم اختبار ركوب دراجة فعلي متحرك (الجهاز كان ثابتاً) - مسار GPS
  المسجل هو ضجيج GPS طبيعي أثناء الثبات (0.21 كم خلال 3:13 دقائق)،
  وليس حركة فعلية. سرعة/مسافة تتبع الحركة الحقيقية لم تُختبر بعد على
  هذا الجهاز في هذه الجلسة (كانت اختُبرت في جلسة Phase 1 الأولى حسب
  ما هو مذكور أعلاه في هذا الملف).
- تم ترك بيانات اختبار حقيقية على جهاز المطوّر: رحلة تجريبية واحدة
  محفوظة في قاعدة البيانات (0.21 كم، 2026/07/10 01:11) و~7.8 ميجابايت
  من بلاطات الخرائط المحمّلة جزئياً (مستويات التكبير 10-13 بشكل كامل
  تقريباً + بعض بلاطات المستويات الأعلى من الاستخدام العادي). لم يتم
  حذف أي منها لأنها ليست تخريبية وقد تكون مفيدة للمطوّر لمعاينتها -
  القرار متروك للمطوّر لحذفها من داخل التطبيق أو عبر `adb shell pm
  clear` إذا أراد جهازاً نظيفاً قبل استخدامه الفعلي.

**التأكيد:** الاختبار أعلاه تم فعلياً على جهاز أندرويد حقيقي متصل عبر
adb (وليس محاكاة/emulator)، مع لقطات شاشة حقيقية وفحص logcat وفحص
ملفات النظام الفعلية على الجهاز كدليل. هذا يرفع درجة الثقة في Phase
1.5 من "تم التحقق عبر flutter analyze/test فقط" إلى "تم التحقق على
جهاز حقيقي فعلياً" - باستثناء اختبار الحركة الفعلية أثناء ركوب دراجة
حقيقي والتحميل الكامل لمنطقة بغداد، وكلاهما يبقيان غير مختبرين حتى
الآن.

**الخطوة التالية:** لا تغيير عن توصية الجلسة السابقة - القرار بين
Phase 2 (BLE) أو تحسينات إضافية على الخرائط (راجع تفاصيل الخيارين في
سجل الجلسة السابقة أدناه) يبقى متروكاً للمطوّر. الاختبار على الجهاز
الحقيقي في هذه الجلسة لم يكشف أي عيب يستوجب العمل الإضافي قبل اتخاذ
ذلك القرار.

---

### جلسة 2026-07-10 - Phase 1.5 مكتملة (خرائط offline + سجل الرحلات)

**الوقت:** 2026-07-10، جلسة واحدة متواصلة.

**ملخص ما تم إنجازه:**

تمت إضافة عرض خرائط offline-first فوق نواة GPS tracking من Phase 1
(لم يتم تعديل أي منطق GPS/tracking core - فقط تمت إضافة الخرائط حوله)،
بالإضافة إلى سجل الرحلات وتفاصيلها.

الملفات الجديدة:
- lib/services/map_tile_service.dart - خدمة الخرائط offline (راجع
  القرار التقني أدناه، مهم جداً)
- lib/screens/download_map_screen.dart - شاشة تحميل خريطة بغداد
- lib/screens/ride_map_view.dart - ويدجت الخريطة الحية (تستخدم في
  tracking_screen)
- lib/screens/ride_history_screen.dart - سجل الرحلات
- lib/screens/ride_detail_screen.dart - تفاصيل رحلة سابقة + خريطة
  المسار الثابتة
- lib/widgets/stat_column.dart - ويدجت الإحصائية المشتركة (تم
  استخراجها من tracking_screen لإعادة الاستخدام في ride_detail_screen)
- lib/utils/format_utils.dart - formatDuration المشتركة
- test/map_tile_service_test.dart - اختبار سلامة إحداثيات بغداد

الملفات المعدّلة:
- pubspec.yaml - إضافة flutter_map, latlong2, http, path_provider, intl
- lib/screens/tracking_screen.dart - إضافة AppBar (أيقونتا تحميل
  الخريطة وسجل الرحلات) + خريطة حية فوق الإحصائيات (Expanded flex 4:6)
  + تعديل الإحصائيات لتكون داخل Center+SingleChildScrollView (لمنع
  RenderFlex overflow على الشاشات الصغيرة بعد تقليص المساحة المتاحة
  لها) + استخراج StatColumn و formatDuration لملفات مشتركة. كل وظائف
  Phase 1 (حالات الأزرار الأربع، عرض السرعة/المسافة/الوقت/الارتفاع)
  ما زالت تعمل بدون أي تغيير في السلوك.
- lib/main.dart - main() أصبحت async، تُنشئ MapTileService وتستدعي
  init() قبل runApp، وتمت إضافتها إلى MultiProvider الموجود (لم يُعَد
  بناء الـ MultiProvider من الصفر).
- android/app/src/main/AndroidManifest.xml - إضافة
  android.permission.INTERNET (كانت موجودة فقط في debug/profile
  manifests للـ Flutter tool؛ لازمة الآن في الـ main manifest لأن
  التطبيق يقوم بطلبات HTTP حقيقية لجلب بلاطات الخريطة في الإصدار
  release).
- test/widget_test.dart - تم تحديثه ليمرر MapTileService (مع مجلد
  مؤقت + MockClient يرفض كل الطلبات) لأن MyApp أصبح يتطلب
  mapTileService كمعامل، ولتجنّب أي طلب شبكة حقيقي أثناء الاختبار.

**القرار التقني الأهم في هذه الجلسة: لم يتم استخدام
flutter_map_tile_caching رغم أنه مذكور صراحة في تعليمات المهمة.**

السبب: عند الفحص، تبيّن أن حزمة flutter_map_tile_caching مرخّصة تحت
GPLv3 (copyleft قوي). بما أن PROJECT_STATE.md نفسه يذكر "Phase 4:
اشتراك مدفوع" كخطة مستقبلية، فإن استخدام حزمة GPLv3 كان سيُلزم
التطبيق بأكمله (بما فيه أي كود مدفوع لاحقاً) بأن يكون مفتوح المصدر
تحت GPLv3، أو شراء ترخيص تجاري من مطوّر الحزمة. تم عرض هذا الخيار على
المطوّر صراحة قبل المتابعة، واختار المطوّر "تجنّب flutter_map_tile_caching
وبناء نظام تخزين مؤقت (cache) بسيط خاص بالتطبيق".

البديل المطبَّق: نظام offline tile caching خاص وبسيط، بدون أي تبعية
خارجية إضافية غير الحزم القياسية (http, path_provider)، مبني مباشرة
فوق TileProvider/ImageProvider الخاصين بـ flutter_map نفسه (وهو MIT
مرخّص، لا مشكلة فيه):
- التخزين: ملفات PNG على القرص في
  `<application support dir>/iraq_cycling_tiles/{z}/{x}/{y}.png`
  (هذا هو "الـ store" المطلوب في تعليمات المهمة، لكن بتنفيذ ملفي بسيط
  بدل ObjectBox الذي تستخدمه FMTC).
- القراءة: offline-first فعلياً - يفحص الملف محلياً أولاً
  (`_OfflineFirstTileProvider.getImage`)، فإن لم يوجد يحاول الشبكة
  (`_CachingNetworkTileImage`، يحفظ الناتج في الكاش تلقائياً بعد
  الجلب)، فإن فشلت الشبكة أيضاً يعرض بلاطة رمادية محايدة 16×16 مُولَّدة
  كـ PNG مُضمَّن في الكود (لا حاجة لملف asset) - لا يحدث أي crash أو
  "error tile" أبداً.
- التحميل المسبق: downloadBaghdadRegion() تحسب كل البلاطات ضمن حدود
  بغداد (33.0-33.6 خط عرض، 44.1-44.6 خط طول) لمستويات التكبير 10-17
  عبر رياضيات slippy-map القياسية، وتُحمّلها بـ concurrency=2 وتأخير
  250ms بين كل دفعة (كإجراء احترازي تجاه سياسة OSM - راجع الملاحظة
  التالية)، مع تخطي أي بلاطة محفوظة مسبقاً.

**ملاحظة سياسة استخدام مهمة (يجب معالجتها قبل أي إطلاق واسع):**
سياسة OSM الرسمية لاستخدام الـ tiles (operations.osmfoundation.org
policies/tiles) تمنع صراحة "bulk downloading". تحميل كل بلاطات بغداد
لمستويات التكبير 10-17 (عشرات الآلاف من البلاطات) هو تحميل جماعي
بالتعريف، حتى لو كان لمستخدم واحد ولمرة واحدة فقط. تم توثيق هذا بشكل
بارز كتعليق في map_tile_service.dart. القرار الحالي: مقبول مؤقتاً
لغرض الاختبار الشخصي من مطوّر واحد فقط (استخدام محدود جداً)، لكن **يجب
قبل أي إطلاق أوسع** استبدال tile.openstreetmap.org بمزوّد مدفوع
(MapTiler, Thunderforest, Mapbox) أو خادم tiles ذاتي الاستضافة، وإلغاء
التحميل الجماعي المسبق نهائياً لصالح تخزين مؤقت عند الطلب فقط. هذا
امتداد لنفس الملاحظة التي طلبتها تعليمات المهمة الأصلية عن "الحجم
الإنتاجي"، لكن الفحص الفعلي للسياسة كشف أن المشكلة أكبر من مجرد حجم:
حتى النسخة الحالية (مستخدم واحد) هي تقنياً خارج نص السياسة، وليس فقط
غير مناسبة للإنتاج.

**قرار تقني إضافي: خيار "استئناف التتبع" (recenter) بدل المتابعة
التلقائية القسرية (Auto-follow vs recenter button - البند المطلوب في
تعليمات المهمة):**
تم اختيار زر "recenter" عائم بدلاً من نافذة زمنية (مثلاً "لا تتبع
تلقائياً إذا حرّك المستخدم الخريطة يدوياً خلال آخر N ثوانٍ"). الخريطة
تتبع الموقع الحالي تلقائياً أثناء التتبع النشط (TrackingState.tracking)
حتى أول لفتة يدوية من المستخدم (onPositionChanged مع hasGesture=true)،
عندها يظهر زر دائري صغير (Icons.my_location) في أسفل يمين الخريطة
لإعادة التفعيل. السبب: نهج زمني (timer-based) يضيف حالة إضافية معقدة
(متى تبدأ العدّاد، متى تُلغى) وسلوكه أقل قابلية للتنبؤ من منظور
المستخدم مقارنة بزر واضح "إما تتبعني أو لا، وأنا أقرر". هذا موجود في
lib/screens/ride_map_view.dart.

**نتيجة flutter analyze:** نجح - "No issues found!"

**نتيجة flutter test:** نجحت 10/10 اختبارات (5 اختبارات Phase 1 في
distance_calculator_test.dart + 1 اختبار widget_test.dart + 4 اختبارات
جديدة في map_tile_service_test.dart تتحقق من صحة حدود بغداد
الجغرافية). لاحظ: تم تعديل widget_test.dart ليمرر MapTileService
مع MockClient (من package:http/testing.dart) بدلاً من عميل HTTP حقيقي،
لتجنّب أي طلب شبكة فعلي أثناء تشغيل الاختبارات.

**التحقق اليدوي (لا يوجد جهاز حقيقي متاح في هذه الجلسة):**
- flutter analyze نظيف تماماً على كامل المشروع (لا تحذيرات، لا أخطاء).
- تمت مراجعة كود تحويل currentRide.points → List<LatLng> في
  ride_map_view.dart وride_detail_screen.dart يدوياً: كلاهما يتعامل مع
  حالة القائمة الفارغة (رحلة بدأت للتو بدون أي نقطة GPS بعد) بأمان -
  PolylineLayer لا يُبنى إلا إذا كان عدد النقاط >= 2، والـ Marker لا
  يُبنى إلا إذا كانت هناك نقطة حالية، وشاشة تفاصيل الرحلة تعرض رسالة
  "لا توجد نقاط GPS مسجلة لهذه الرحلة" بدل عرض خريطة فارغة أو الانهيار.
  لا يوجد أي `!` (force-unwrap) غير آمن على قيم قد تكون null في هذا
  المسار.
- تم اكتشاف وإصلاح خطأ حقيقي أثناء flutter test (وليس نظرياً فقط):
  TileLayer الخاص بـ flutter_map يستدعي
  `tileProvider.headers.putIfAbsent(...)` داخلياً، وكانت خرائط الـ
  headers التي مررتها في map_tile_service.dart أصلاً `const` (غير
  قابلة للتعديل) مما تسبب في `UnsupportedError: Cannot modify
  unmodifiable map` عند بناء أي شاشة تحتوي على الخريطة. تم إصلاحه
  بإزالة `const` عن خارطة الـ headers الممررة لـ TileProvider.
  هذا يوضح قيمة تشغيل flutter test فعلياً وليس الاكتفاء بـ analyze.
- تم اكتشاف وإصلاح RenderFlex overflow حقيقي في tracking_screen.dart:
  بعد تقليص مساحة الإحصائيات لإفساح المجال للخريطة، لم يعد المحتوى
  (رقم السرعة 96px + الإحصائيات) يتّسع دائماً ضمن المساحة المتبقية على
  الشاشات الأصغر (ولوحظ فعلياً بحجم نافذة اختبار افتراضي 800×600). تم
  إصلاحه بلف المحتوى بـ Center + SingleChildScrollView بدل
  Column(mainAxisAlignment: center) مباشرة، بحيث يتوسّط المحتوى عند
  توفر مساحة كافية (معظم الهواتف) ويصبح قابلاً للتمرير بدل الانهيار/
  التجاوز عند ضيق المساحة، بدون تغيير أي حجم خط أو قيمة معروضة.

**مشاكل غير محلولة:**
- لم يُختبر التطبيق فعلياً على جهاز حقيقي في هذه الجلسة (لا يوجد جهاز
  متصل). كل التحقق تم عبر flutter analyze + flutter test + مراجعة
  الكود يدوياً.
- مسألة سياسة OSM لتحميل بلاطات بغداد الجماعي (راجع الملاحظة أعلاه)
  لم تُحل نهائياً - القرار الحالي مقبول فقط للاستخدام الشخصي المحدود
  الحالي، ويجب إعادة النظر فيه قبل أي إطلاق أوسع.
- لم يتم اختبار مسار "لا يوجد إنترنت فعلياً + لا توجد بلاطة محفوظة"
  على جهاز حقيقي (البلاطة الرمادية البديلة تم التحقق من صحتها برمجياً
  فقط - أنها PNG صالح 16×16 - وليس بصرياً على شاشة حقيقية أثناء ركوب
  فعلي).
- لم يتم قياس الحجم الفعلي بالميجابايت لتحميل منطقة بغداد الكاملة على
  جهاز حقيقي (فقط تقدير حسابي تقريبي معروض في download_map_screen).

**الخطوة التالية (لأي جلسة قادمة، بدون أي سياق مسبق):**
القرار متروك للمطوّر بين مسارين:

1. **Phase 2 (تكامل BLE + حساب سعرات حرارية):** استخدام
   flutter_reactive_ble (مذكور في المعمارية المتفق عليها أعلى هذا
   الملف) للاتصال بحزام نبض القلب وعداد قدرة عبر BLE، وإضافة حساب
   للسعرات الحرارية المحروقة بناءً على نبض القلب/القدرة/الوزن. هذا هو
   الامتداد الطبيعي التالي حسب خطة المراحل الأصلية.

2. **تحسينات إضافية على الخرائط (إن أراد المطوّر البقاء في نطاق
   الخرائط قبل الانتقال):**
   - اختبار فعلي على جهاز Android حقيقي: تثبيت التطبيق، تجربة "تحميل
     الخريطة" فعلياً والتأكد من نجاح التحميل وعرض البلاطات المخزّنة
     أثناء وضع الطيران (airplane mode).
   - قياس الحجم الفعلي لمنطقة بغداد بعد التحميل الكامل ومقارنته
     بالتقدير الحسابي في download_map_screen.dart.
   - معالجة مسألة سياسة OSM (الملاحظة أعلاه) بجدية أكبر: إما التسجيل
     للحصول على إذن مسبق من OSM، أو التحول لمزوّد مدفوع الآن بدل
     تأجيله لما بعد الإطلاق.
   - إضافة زر لحذف/إعادة تعيين الكاش المحلي من داخل download_map_screen
     (غير موجود حالياً - المستخدم لا يستطيع حالياً تفريغ الكاش من
     داخل التطبيق).

كل من المسارين لم يُبدأ فيه أي عمل بعد.

**ملاحظة git:** لم يتم تنفيذ أي git commit أو push في هذه الجلسة، حسب
تعليمات المهمة الصريحة. كل التغييرات أعلاه غير مضمَّنة (uncommitted)
بانتظار المراجعة اليدوية من المطوّر.

**تأكيد:** تم كتابة هذا القسم فعلياً إلى PROJECT_STATE.md على القرص
(وليس فقط وصفه في ملخص المحادثة) - تحقق من ذلك عبر أداة القراءة بعد
الكتابة إذا لزم الأمر.

---

### جلسة 2026-07-09 (الأولى) - Phase 1 مكتملة

تم بناء: lib/models/ride_point.dart, lib/models/ride.dart,
lib/utils/distance_calculator.dart (Haversine formula),
lib/services/location_service.dart, lib/services/ride_tracker.dart
(ChangeNotifier), lib/services/ride_repository.dart (sqflite,
rides + ride_points tables with FK CASCADE), lib/screens/tracking_screen.dart
(Arabic UI, 96px speed display), lib/main.dart (MultiProvider setup).

AndroidManifest.xml: added ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION.
minSdkVersion: already 21 by default in Flutter 3.32.0, no change needed.

flutter analyze: نجح - "No issues found!"
flutter test: نجحت 5/5 اختبارات. ملاحظة مهمة: تم تصحيح قيمة اختبار
المسافة المتوقعة بين بغداد والبصرة من 420كم (خاطئة) إلى 448.56كم
(دقيقة، تم التحقق عبر Air Miles Calculator وTravelmath وGeodatos)
لأن 420كم كانت تمثل مسافة الطريق البري وليس المسافة الجغرافية
الفعلية (great-circle) التي تحسبها صيغة Haversine.

Git: تم تشغيل git init لاحقاً من قبل المطور يدوياً (لم يكن موجوداً
في الجلسة الأولى). لا commits حتى الآن - كل التغييرات بانتظار
المراجعة اليدوية.

مشكلة معروفة: هذا الملف (PROJECT_STATE.md) كان من المفترض إنشاؤه
في نهاية الجلسة الأولى ولم يُنشأ فعلياً رغم ذكره في الملخص. تم
إنشاؤه الآن يدوياً في جلسة تصحيحية منفصلة بتاريخ نفس اليوم.

الخطوة التالية: اختبار Phase 1 على جهاز أندرويد حقيقي عبر
adb install، للتأكد من عمل GPS tracking فعلياً أثناء الحركة. بعد
التأكيد، الانتقال لـ Phase 1.5 (عرض المسار على خريطة) أو مباشرة
Phase 2 (BLE) - القرار يعود للمطور.
</content>
